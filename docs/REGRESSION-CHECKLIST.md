# INSTANCE-ISOLATION-20260828｜A-D 独立支线、桌面实例与账号隔离

- [x] 复现条件：多个调试/生产窗口共用旧的账号列表、浏览器分区或运行目录时，一个窗口的修改、恢复或登录态可能干扰另一窗口。
- [x] 根因：旧实例模型只提供双实例映射，启动入口没有把账号、端口、运行目录、Electron `userData` 和单实例锁作为同一隔离元组固定下来。
- [x] 修复：实例策略扩展为 A-D 单账号映射；`start-instance-a.ps1` 至 `start-instance-d.ps1` 显式绑定唯一账号与独立运行/浏览器目录；桌面锁命名空间按实例隔离；服务端和网页预览按当前实例动态识别账号。
- [x] 自动回归：`node --test --test-concurrency=1 instance-startup.test.js public/workbench-ui.test.js`，506 项中 505 通过、1 跳过、0 失败；启动脚本回归额外校验四实例只共享素材根目录。
- [ ] 桌面验收：本轮只做静态隔离验证，未启动 B/C/D、未登录或迁移账号、未发送 GPT 请求；后续须在用户确认且安全窗口内分别启动实例，核对端口、userData、页面账号和实际日志，再做最小真实生产验收。

# GPT-B-PREVIEW-GUARD-20260828｜B 网页预览全页只读保护

- [x] 复现条件：B（`127.0.0.1:4332`）网页预览中，账号管理器位于设置区域，不属于 `#gptProductionTestView`；原事件捕获条件可能让该区域绕过只读保护。
- [x] 根因：事件捕获器在命中 GPT 控件后再次要求祖先位于生产视图，导致生产视图外的 GPT 账号管理控件未进入阻断分支。
- [x] 修复：只读事件门禁改为覆盖整页 GPT 控件；动态重绘、隐藏/禁用控件和直接生产入口门禁继续生效。
- [x] 自动回归：`node --check src/public/app.js`；`node --test src/public/workbench-ui.test.js`，502 项中 501 通过、1 跳过、0 失败。
- [x] B 页面验收：两个 B 预览标签热刷新后均为只读，账号管理、上传、模板、浏览器导航和恢复控件隐藏或禁用，账号标签仅为 3/4，控制台错误为 0。
- [ ] 真实生产验收：GPT 网页桥接仍未就绪；不能把网页只读预览验收当成账号 3/4 真实生产通过，待用户完成 B Electron/GPT 桥接人工确认后再验收。

# GPT-LONG-CONVERSATION-INSPECT-196｜长对话巡检预算校准（0.19.196）

- [x] 1号现场扩展消息实测 10.433 秒返回正确 `plan-ready`。
- [x] 扩展巡检回复预算 15 秒、总调用预算 18 秒，保持有限等待。
- [x] 所有阶段识别调用统一使用巡检预算，普通 API/状态探测保持原预算。
- [x] 专项回归通过。
- [x] 重启加载后内部 `inspectStatus(account-4)` 返回 8页计划并自动收养。

# GPT-STALE-QUARANTINE-TAIL-195｜队尾历史隔离不再阻断新会话边界（0.19.195）

- [x] 启动隔离门继续检查当前队列游标任务。
- [x] 启动隔离门继续检查运行真源 `currentTaskId`。
- [x] 已越过队尾的历史 `lastFailedTask` 不再阻断页面新计划识别。
- [x] 定向 UI 回归通过：454 passed / 1 skipped / 0 failed。
- [x] 重启后 1号已收养“苏州西山岛一日团建”P1-P8，下一步仅发送一次 `1`。

# GPT-SHORT-MATERIAL-BOUNDARY-194｜简化附件消息边界识别（0.19.194）

- [x] 完整“请读取全部附件 + 当前素材文件夹”仍识别为素材边界。
- [x] “TXT/文档等附件标签 + 当前素材文件夹”的恢复消息识别为最新素材边界。
- [x] 只有“当前素材文件夹”的普通讨论不触发生产。
- [x] 桌面重启后1号现场检查为最新西山岛计划、8页、等待确认，不再拼接上一套 `1`。

# GPT-FRESH-RECOVERY-BUDGET-193｜新会话恢复预算重置（0.19.193）

- [x] 失效旧 `/c/...` 转入 `freshConversationBootstrap` 时，未提交任务清除窗口恢复、自动恢复和页面就绪恢复计数。
- [x] 新 `/c/...` 创建成功后再次清理旧环境预算，再移除新会话引导标记。
- [x] 已提交任务、强制上传边界、素材生命周期锁和额度记录不被预算重置越过。
- [x] `node --check src/public/app.js` 与定向 UI/恢复回归通过。
- [x] 桌面重启加载 0.19.193 后，账号 2 同一未提交任务仍保留 8 条队列和失效 URL 标记，但旧窗口/自动/就绪恢复计数及 `RESTART_INTERRUPTED` 错误均已清空。
- [ ] 08:00 后桌面验收：账号 2 首任务建立新 `/c/...`，未因旧的 6 次恢复计数立即耗尽，并出现上传/计划/归档真实进展。

# GPT-STALE-CONVERSATION-191｜失效会话链接防复活（0.19.191）

- [x] 复现条件：未发送任务绑定的旧 `/c/...` 重定向到 ChatGPT 首页；账号档案被清空后，历史会话日志又恢复同一链接。
- [x] 根因确认：历史日志回退缺少账号级失效 URL 过滤，导致新自动任务在首页再次绑定旧链接。
- [x] 自动回归：完整 `npm test --silent` 为 1038 项，1037 通过、0 失败、1 跳过。
- [x] 桌面验收：0.19.191 连续两次重启后，账号 2 保持首页、登录和扩展就绪；旧链接仍在失效标记中，`allowLogFallback` 返回空，8 条队列与未发送首任务保持，未重复上传。
- [ ] 工作时段验收：08:00 后确认账号 2 从首页建立真实新 `/c/...`，进入上传/计划阶段并产生归档；停产时段不越界发送。

# GPT-STABLE-IMAGE-179｜生图阶段稳定完成门（0.19.179）

- [x] 静态回归：生成中即使图片节点达到计划数，也保持“等待图片”与 48% 阶段。
- [x] 单测：文案有限重试耗尽后，原对话出现符合协议的最终文案时优先采用，不重复发送。
- [ ] 桌面验收：生成信号结束后只进入一次文案阶段，并继续下载、打包、归档。

# GPT-OWNER-178｜上传前任务归属锁（0.19.178）

- [ ] 当前对话上一套未归档时，下一套不得读取或上传附件。
- [ ] 另一自动任务遗留的提示词和附件不得由当前作品提交。
- [ ] 用户人工输入仍是硬边界，不得自动清理。

# GPT-PLAN-177｜可见计划优先接管（0.19.177）

- [ ] 当前素材消息后已经出现完整 P1-Pn 与“等待回复 1”时，不再发送“继续处理附件”。
- [ ] 计划在静默恢复边界刚好完成时，记录 `plan-visible-race-adopted` 并进入确认步骤。
- [ ] 可见计划不完整或超过 10 页时，仍执行原有有限恢复与页数校验。

# GPT-RECOVERY-176｜恢复耗尽精确隔离（0.19.176）

- [x] 静态回归：隔离优先查找本次 requestId，而不是旧 `lastFailedTask`。
- [ ] 桌面验收：同一 requestId 只出现一次 recovery exhausted，随后队列继续。

# GPT-RECOVERY-175｜同会话加载与新 worker 防误杀（0.19.175）

- [x] 单测：最近 30 秒有真实进展时不允许边界探针停止 worker。
- [x] 静态回归：同会话 `did-start-loading` 不再无条件中断 pending 请求。
- [ ] 桌面验收：1–3 号恢复后跨过 30 秒仍无误中断，并继续到图片/文案/归档。

# GPT-RECOVERY-174｜活跃窗口阻止旧恢复回调刷新（0.19.174）

- [x] 静态回归：`controlledRecovery` 不再绕过 `activeWorker.autoRunning`。
- [ ] 桌面验收：发送 `1`/等待图片后，旧恢复回调不会刷新或中断当前任务。

# GPT-HOLD-173｜旧巡检安全暂停自动迁移（0.19.173）

- [x] 单测：启用的单账号自动窗口会释放明确 `inspection-safety-hold:` 旧暂停。
- [x] 单测：真实用户停止、停用账号和手动模式不会被释放。
- [ ] 桌面验收：1–3 号从检查点自动恢复并产生真实阶段推进/归档；4 号停用和 6 号禁用保持不动。

# GPT-BRIDGE-172｜活跃任务桥接防自杀与附件幂等恢复（0.19.172）

- [x] 活跃生产请求遇到连续桥接读取超时，不中断任务、不触发页面重建。
- [x] 成功读取清零桥接超时累计；CDP 只在该读取通道已经超时后启用。
- [x] 已知稳定 `/c/...` 对话的一次读取超时不会触发刷新。
- [x] 同一任务的提示词和附件仍在 composer 时直接提交，不重复上传。
- [x] 自动回归：1029 项，1028 通过、0 失败、1 项跳过。
- [ ] 桌面验收：1–3 号在 0.19.172 上出现真实阶段推进/归档，且无活跃任务 `gpt-task-aborted`。

# GPT-RECOVERY-171｜多窗口立即并行与旧检查点解锁（0.19.171）

- [x] 默认关闭旧启动错峰，启用账号可同时进入恢复/生产。
- [x] 无账号归属的旧检查点不能永久阻断补仓。
- [x] 有唯一同账号证据的未完成检查点继续阻止新素材发送。
- [ ] 桌面验收：2、3 号从“等待素材”进入真实队列并通过网页阶段核验。

# GPT-RECOVERY-170｜空队列先恢复原作品（0.19.170）

- [x] 自动窗口空队列时，耐久检查点恢复调用早于新素材选择。
- [x] 未完成原会话仍阻止新素材注入，不会用“等待素材”掩盖在途作品。
- [ ] 桌面验收：重启后 1–3 号恢复原 `/c/...` 并出现真实阶段进展或新归档。

# GPT-RECOVERY-169｜同会话标题变化安全恢复（0.19.169）

- [x] 桌面桥接携带权威会话 URL 与完整账本所有权确认。
- [x] 页面恢复只在同账号、同请求、同 `/c/...` 会话且所有权已确认时允许标题变化。
- [x] 标题冲突但缺少所有权证据时仍保持阻断。
- [ ] 桌面验收：重启后账号 2 在原会话继续，不再出现标题导致的 `CONVERSATION_OWNER_MISMATCH`，且无重复发送 `1`。

# GPT-RECOVERY-168｜账号隔离与原作品身份续接（0.19.168）

- [x] 重试当前步骤与重启恢复保留已提交作品的原 requestId，恢复 attempt 独立记录。
- [x] 新检查点持久化 `accountWindowId/accountId`；歧义恢复按 requestId 查询完整会话账本，旧检查点、同素材兜底和孤儿恢复必须有同账号证据。
- [x] 已提交非终态作品不会因旧归档页面而清空 workflow、跳新会话或重复上传。
- [x] 认领任务后将权威检查点 `/c/...` URL 回写到 task，当前错误页面不能覆盖原会话。
- [x] 缺少生图回复边界先有限重试；旧版同类误隔离只恢复一次，其他隔离不越过。
- [x] 自动回归：1025 项，1024 通过、0 失败、1 跳过。
- [ ] 桌面验收：0.19.168 重启后回到账号 2 原 `/c/...` 会话，保持原 requestId；核对无重复确认/上传并出现真实阶段推进或新增归档。

# GPT-PROGRESS-112｜工作时段优先于错峰调度（0.19.112）

- [x] 连续生产调度先判断工作时段，再判断前置窗口和启动错峰。
- [x] 工作时段外只写入 `waiting-schedule` 与下一唤醒时间，不消耗错峰启动名额。
- [x] `workbench-ui.test.js` 静态回归覆盖调度顺序，防止工作时段外进入错峰等待。
- [ ] 桌面验收：仅在安全空档加载 0.19.112，并在下一工作窗口确认 1 号先启动、后续每条间隔 5–10 分钟；工作时段内不强制打断。

# GPT-PROGRESS-111｜真实进度心跳与有限等待证据（0.19.111）

- [x] `writeGptWindowRuntime` 对进度字段做新旧值比较；相同轮询不刷新 `lastProgressAt`。
- [x] 单账号与独立窗口轮询均不再直接写入当前时间冒充推进。
- [x] `workbench-ui.test.js` 静态回归覆盖比较逻辑与移除假推进写入。
- [x] 启动定时器先判断工作时段，只有真实进入 worker 才调用 `markGptStartupLaunched`。

# GPT-STARTUP-109｜工作时段切换不消耗启动错峰预算（0.19.109）

- [x] 复现条件：应用在 02:00 后启动，四个账号因工作时段等待；旧错峰兜底在 10 分钟后把未实际启动的前置窗口标成已释放，次日 07:00 可能同时放行。
- [x] 根因确认：启动错峰的有限超时与工作时段边界共用同一内存状态，超时释放没有区分“真实启动”和“非工作时段等待”。
- [x] 修复：进入新的 07:00—02:00 工作窗口时，若上一轮没有任何窗口真实 `launchAt`，清除合成 `released`/旧延迟；1 号先启动，后续按前一窗口真实启动后的 5–10 分钟随机延迟。已有真实启动则保留当前节奏。
- [x] 自动回归：`gpt-startup-stagger.test.js` 覆盖预工作时段超时重置和真实启动跨窗口保留；`workbench-ui.test.js` 覆盖工作台调用重置逻辑。
- [x] 桌面验收：安全空档加载 0.19.109；API 返回 0.19.109；四个生产页仍为各自原始 `/c/...` 会话；运行态显示 `continuousAutoStart=true`、四窗口 `armed=true`，账号 6 保持停用。北京时间 07:00 后仍需以实际 `launchAt`、worker 和归档推进日志完成白天生产验收。

# CONVERSION-096｜全链路知识库与上帝视角工作区（0.19.65）

- [x] 流量转化左侧入口显示“全链路知识库”，不再显示“HTML复盘”；来源 HTML 仅保留为二级追溯入口。
- [x] 知识模块按执行摘要、全量漏斗、成交分析、避坑指南、转化 SOP、客户百问百答、AI 自动化、团队分析、上帝视角、工作台同步、方法论沉淀展示，共 11 个 Tab。
- [x] 成交案例与成交产出合并为“成交分析”；工作台同步展示正式层 / 候选层 / API / 最近复核，候选 `formalWrite=false` 时不写正式层。
- [x] 真实 4327 重载后验收：运行版本 `appVersion=0.19.65`；点击流量转化 → 全链路知识库，核对上帝视角经营卡片、工作台同步边界、11 个 Tab；浏览器错误日志为空。
- [x] 回归命令：`node --check src/public/app.js`；核心流量转化 / 版本 / 帮助断言通过；`git diff --check` 通过。完整工作台回归仍有 1 个既有 `src/desktop/preload.js` 旧签名断言失败，未修改其脏工作区。
- [x] 技能中心维护边界：卡片显示“流量转化模块维护”，全链路知识库标记为模块内派生展示，维护流程包含“校准上帝视角 → 更新流量转化模块”；live UI 实测来源材料显示 7 项进入工作台，并包含“工作台同步”。
- [x] 服务子进程安全重载：`/api/skills` 实测 200，维护技能来源模块为 11 项并包含 `sync`，安全边界返回“来源材料缺失”，与源码一致；未打断 GPT 生产窗口。

# CONVERSION-095｜成交产出分月与毛利区间（0.19.62）

- [x] HTML复盘包含“成交产出”模块和可点击模块入口；不再只展示抽象的成交案例动作。
- [x] 固定核算口径：11 单硬证据成交、总产出 308,263 元，按执行月份展示 2026-06/07/08/09；6 月与 9 月无确认产出，8 月注明截至 21 日。
- [x] 毛利区间按 10%–25% 计算为 30,826–77,066 元，直接成本区间为 231,197–277,437 元，并明确估算边界。
- [x] WeFlow 实时 API 不可用时，页面保留待补同步提示；不把 `won_signal`、报价或计划金额写成确认成交。
- [x] `node --check src/public/app.js`；`node --test src/public/workbench-ui.test.js src/public/skills-ui.test.js`；`git diff --check`。

# XHS-TEMPLATE-COLLECTOR｜聚光作品右下角采集按钮（0.19.65）

- [x] 用户脚本限定匹配 `https://ad.xiaohongshu.com/microapp/creativity/inspire*`，动态卡片通过 `MutationObserver` 绑定右下角按钮，不打开笔记、不读取 Cookie。
- [x] 卡片读取标题、24 位笔记 ID、图片槽位和点赞/收藏/评论，并以 `xhs:<noteId>` 写入持久去重账本；没有真实短链时只登记缺口，不启动空下载任务。
- [x] 卡片识别对 `data-note-id`、`data-id`、`/explore/<noteId>` 链接和标题备用选择器有兜底，页面 DOM 小幅变化不会直接失去按钮。
- [x] `POST /api/template-collector/queue` 的无效输入返回 `400`；`GET /api/template-collector/ledger` 返回账本摘要；小红书聚光来源 CORS 已加入允许列表。
- [x] `node --test src/lib/xhs-template-collector.test.js src/server/routes/skills.test.js`：24/24；三个入口 `node --check` 通过；`git diff --check` 无空白错误。
- [x] Edge 隔离卡片验收：在 Edge 本地 HTTP 隔离页加载同一用户脚本，按钮成功注入，点击后变为 `✓` 并返回“已加入本地模板仓库下载队列”；正式聚光页仍待用户安装 Tampermonkey/Violentmonkey 并显示真实卡片。
- [ ] Edge 真实聚光卡片验收仍待完成：当前 Edge 未检测到 Tampermonkey/Violentmonkey，且正式页没有可读卡片；不可把隔离页/单元测试当成真实账本写入验收。

# PUBLISHING-101｜五平台统一官方网页工作区（0.19.56）

- [x] 左侧继续读取现有成品库，右侧统一渲染当前平台官方网页工作区；旧本地公众号编辑器不再作为在线分发可见右侧。
- [x] 公众号、小红书、抖音、X / 推特和携程旅行五个平台均有官方入口；平台切换后标题、地址和当前平台状态同步刷新。
- [x] 平台切换重新计算左侧当前平台资格；抖音/小红书共享使用状态，携程使用后不再重复进入携程候选。
- [x] 浏览器实测五个平台逐一点击；隔离最新版源码实际扫描到 42 个作品集、13 篇帖子，并验证小红书/携程资格数量变化。
- [x] 定向回归：在线平台相关 13/13；`node --check public/app.js desktop/main.js desktop/preload.js`；`git diff --check`。
- [ ] 桌面最终落地：当前 4327/Electron 仍保留 GPT 生产现场，待安全空档重启后确认五个平台原生 WebContentsView 在桌面窗口实际挂载；不得强制打断当前生产。

# SKILLS-091｜朋友圈采集回执修复与最新版源码验收（2026-08-16）

## GPT-100｜当前对话文案边界阻止旧生图恢复（0.19.50）

- [ ] 旧检查点缺少 `textSubmitted`，但当前对话已有同一素材的文案请求轮次时，不发送补图提示。
- [ ] 恢复后将 `textSubmitted` 和文案边界重新写入检查点，并进入 `wait-copy`/归档链。

## GPT-099｜文案完成后的旧生图恢复闸门（0.19.49）

- [x] 日志复现：同一 request 在 `copy-format-recovery-received` 之后又进入 `wait-images`，重复发送 6/8 补图提示。
- [x] 根因确认：文案成功没有及时落检查点，恢复从步骤数组头部执行且图片等待没有文案边界判断。
- [x] 自动回归：`gpt-automation-core.test.js` 覆盖已有 `copyText` 或文案恢复阶段时禁止旧 `wait-images` 补图。
- [x] 语法回归：扩展核心与侧边栏 `node --check` 通过。
- [ ] 桌面验收：账号 `4-embrace.ping` 当前为用户停止状态；待安全空档加载 0.19.49 后，不发送新消息，核对同一 request 不再新增 `partial-image-recovery-sent`。

## GPT-098｜启动时序与缓存维护边界（0.19.46）

- [x] 启动后 60 秒内不清理临时网页缓存，给账号窗口恢复和 worker 挂接留出时间。
- [x] 清理执行前再次复核 worker 占用，生产中不会清缓存。
- [x] 新增启动宽限回归；全量 `npm test --silent` `807/807`，`node --check` 通过。

## GPT-097｜恢复请求生命周期与缓存维护边界（0.19.45）

- [x] 日志确认：心跳超时后的旧桥接请求未释放，恢复请求撞上 `GPT_REQUEST_IN_FLIGHT`；账号 2/4 的重复中止与该生命周期问题一致。
- [x] 心跳失联先停止当前请求并等待旧 Promise 收尾；停止当前窗口只按请求号释放对应账号的请求。
- [x] 全局临时缓存维护从 10 分钟改为 3 小时；07:00–02:00 外等待，任一账号生产中暂缓，不清理登录存储。
- [x] `lib/gpt-page-recovery.test.js`、`lib/temporary-web-cache-schedule.test.js` 与新增工作台回归通过。
- [x] 全量 `npm test --silent` `806/806`，`node --check` 通过。
- [ ] 账号 2 当前网页检查点自然恢复后，继续观察 `GPT_REQUEST_IN_FLIGHT` 与 `gpt-task-aborted` 是否收敛；不强制刷新正在生图的 GPT 页面。

## GPT-096｜侧边栏手动上传按窗口独立且可反馈（0.19.44）

- [x] 复现确认：全局 `gptAutoRunning` 会把侧边栏上传按钮原生置为 `disabled`；账号 3 日志另有手动请求被 `gpt-page-load-stalled` 触发的刷新中止，后续点击返回 `GPT_REQUEST_IN_FLIGHT`。
- [x] 素材和模板上传按当前账号窗口计算锁定；运行中点击显示暂停提示，空闲账号可上传；其他账号运行不再锁死当前窗口。
- [x] 模板目录和上传入口重复定义已收敛；缺失条目、请求占用、页面恢复均有明确提示或安全等待。
- [x] `node --check public/app.js desktop/main.js` 通过；相关回归 `327/327`；全量 `npm test --silent` `801/801`。
- [ ] 待账号 3 网页恢复到可识别会话后，现场确认一次真实本地模板上传；当前仍保留已有队列和账号登录状态，不盲传到未知页面。

## GPT-095｜GPT 网页识别超时不再刷新空转（0.19.43）

- [x] 根因复现已由 `desktop.log` 确认：`gpt-task-aborted` 与 `gpt-page-load-stalled` 交替出现，心跳恢复分支直接刷新活动任务页面。
- [x] 活动 worker 的心跳恢复改为在当前 runner 内等待网页就绪并从检查点重试；新素材在轻量状态就绪但完整识别超时时允许直接上传。
- [x] 自动刷新按账号限制为 10 分钟内最多 2 次，计数写入运行态并跨工作台刷新保留；主进程单次加载恢复仍最多 2 次。
- [x] `node --check public/app.js desktop/main.js` 通过；生产界面定向回归 `300/300`，相关模块 `321/321`，全量 `npm test --silent` 为 `800/800`。
- [x] 4327 `/api/runtime-info` 已回读 `0.19.43`；工作台资源已回读 `/app.js?v=0.19.43`。未刷新 GPT 账号页，账号 4 仍保持独立运行。
- [ ] 待下一套素材自然进入账号 2 的就绪会话后，现场确认“完整识别超时但输入框就绪”路径实际上传；当前账号 2 仍为真实网页加载超时，不能盲传到未知页面。

## CONVERSION-094｜HTML复盘模块与维护技能入口（2026-08-18）

- [x] 右侧流量转化入口显示“HTML复盘”，页面不再使用“全链路知识库”作为主标题。
- [x] 原 HTML 的执行摘要、漏斗、成交、避坑、SOP、百问百答、AI、团队、上帝视角和维护方法可在页面内切换查看。
- [x] 页面显示“流量转化模块维护”技能的真实运行顺序，并提供跳转技能中心和打开原始 HTML 的按钮。
- [x] `node --check public/app.js` 通过；`node --test public/workbench-ui.test.js public/skills-ui.test.js` 为 `319/319`。
- [ ] 浏览器刷新后人工确认模块切换与技能中心定位；不运行真实聊天写入，不触发外部发布。

## PERF-092｜整机卡顿最小修复验收（0.19.26）

- [x] `node --check desktop/main.js`、`node --check server.js`、`node --check public/app.js` 通过；`git diff --check` 通过。
- [x] 内容制作页使用 `/api/distribution/live?summary=1`，轮询有单请求互斥、超时和 `AbortController`；其他页面仍保留完整快照。
- [x] GPT 已展开素材树的定时刷新使用 `loadMaterialCategory`，不再每 15 秒调用完整 `loadDashboard`。
- [x] 运行版本探针 `/api/runtime-info` 与静态 `app.js?v=` 兜底已覆盖；检测到持久化 GPT 任务时不自动刷新。
- [x] GPT 只读探测连续 3 次超时后退避 5 秒，控制指令路径不退避。
- [x] `npm test`：781/781；工作台定向回归和 GPT 桥接回归通过。
- [ ] 旧 4327 进程仍在生产中，待账号任务进入安全空档后重启，验收运行 URL 为 `appVersion=0.19.26`、`/api/runtime-info` 为 200，并观察生产续跑与超时日志是否收敛。

## MOMENTS-093｜朋友圈月采集首选日与补采窗口（0.19.24）

- [x] 默认每月 1 号优先；1 号之后在配置补采窗口内仍可触发，窗口外不触发。
- [x] 新安装默认月度采集时间为 `10:20`、每日准备时间为 `10:20`；两项在技能设置中分别保存，重新打开后仍能回读。
- [x] 同一天失败/缺少目标只尝试一次，次日可继续；成功后当月不重复。
- [x] 设置中心可保存补采天数，面板状态显示首选日、补采窗口和当前素材库。
- [ ] 真实 WeFlow 采集仍需明确账号/UID 后现场验收；本轮只做代码和离线回归，不打开微信、不采集真实朋友圈。

## MOMENTS-092｜朋友圈智能回退、定时状态与发送前自检（0.19.22）

- [x] Node 定向回归覆盖上海时区下一次触发时间、朋友圈筛选级联、技能中心入口和只读自检按钮。
- [x] Python Moments 回归覆盖“去年今天 → 去年本月随机 → 历史同月随机”级联和稳定日内顺序。
- [x] 发送前自检只调用 `prepare-today --dry-run`；不启动微信、不写发布状态、不移动原素材。
- [ ] 到达真实配置时间后再验收一次后台定时触发；错过时间不补发是当前明确设计。
- [ ] 真实微信桌面发布仍需人工现场验收；最终“发表”必须由用户点击。

- [x] `npm test --silent`：`687/687`，失败 `0`；Node 主工作台、技能中心、朋友圈路由与采集回执回归全部通过。
- [x] 项目专用 `src/.venv-moments`：`moments_library` `25/25`、`moments_publisher` `2/2`；使用 `pywechat127==1.9.8` 环境复验。
- [x] `node --check public/app.js`、`node --check server.js` 通过；`VERSION`、Node 包、静态资源缓存参数均为 `0.18.91`。
- [x] 4327 主应用与 8765 流量转换服务保持在线；朋友圈采集多行 JSON 回执解析已覆盖完整 JSON、诊断前缀和单行 JSON 三种形态。
- [x] 未把 `artifacts/`、日志、`.bak-*` 备份或本地虚拟环境纳入源码提交；朋友圈最终发表仍由人工确认。

# SKILLS-089｜技能中心与正式资源版本收尾验收（2026-08-15）

- [x] `npm test --silent`：`679/679`，失败 `0`；GPT 额度预检、可编辑主提示词、技能中心和朋友圈相关回归全部通过。
- [x] `node --test public/skills-ui.test.js public/workbench-ui.test.js server/routes/skills.test.js`：`282/282`。
- [x] `VERSION`、Node 包、便携版输出目录和 `index.html` 全部静态资源缓存参数均为 `0.18.89`；4327 根页面与 8765 健康接口 HTTP 200。
- [x] 正式浏览器刷新后技能中心为 13 张卡、三列、首卡约 `263×161px`；详情/流程/安全默认隐藏，悬停超过 3 秒出现六问说明。
- [x] 浏览器业务错误日志为空；未执行素材移动/清理、模板真实采集、聊天正式写入或外部发布，朋友圈最终发表仍由人工确认。

# SKILLS-088｜正式版本承接技能卡紧凑化验收（2026-08-15）

- [x] `npm test --silent`：`678/678`，失败 `0`；版本一致性、技能卡、主工作台和朋友圈相关回归全部通过。
- [x] `node --test public/skills-ui.test.js public/workbench-ui.test.js server/routes/skills.test.js`：`281/281`；技能卡紧凑、三秒说明、技能路由和打包目录通过。
- [x] `VERSION`、Node 包、便携版输出目录和静态资源缓存均为 `0.18.88`；4327 根页面 HTTP 200，8765 健康接口 HTTP 200。
- [x] 正式浏览器刷新后技能中心为 12 张卡、三列、首卡约 `263×161px`；详情/流程/安全默认隐藏，悬停超过 3 秒出现六问说明。
- [x] 页面错误日志为空；未执行素材移动/清理、模板真实采集、聊天正式写入或外部发布。

# SKILLS-087｜技能卡紧凑化与三秒说明正式验收（2026-08-15）

- [x] `node --test public/skills-ui.test.js public/workbench-ui.test.js`：`271/271`。
- [x] `node --test server/routes/skills.test.js`：`9/9`；技能目录、模板仓库、素材处理设置和秘密边界通过。
- [x] `npm test --silent`：`677/677`；本次新增“技能卡默认紧凑，详细内容只交给三秒悬浮提示”回归通过。
- [x] 正式页面刷新后资源查询为 `0.18.87`；4327 主应用 HTTP 200，技能目录显示 12 张卡。
- [x] 当前视口实测首卡约 `263×161px`、三列排列；流程、详情、安全和连接说明默认 `display:none`，卡片仅保留索引信息和操作入口。
- [x] 指针实测停留超过 3 秒后出现六段延迟说明；卡片详情没有变成长内容，离开后说明关闭。
- [x] 内容制作主操作栏保持模式选择与主操作双列，避免宽屏下不必要的整行占用。
- [x] 没有执行素材移动/清理、模板真实采集、聊天正式写入或外部发布；当前用户确认边界保持不变。

# SKILLS-086｜模板仓库技能一键执行与最终正式页面验收（2026-08-15）

- [x] `node --test public/skills-ui.test.js public/workbench-ui.test.js`：`270/270`。
- [x] `node --test server/routes/skills.test.js`：`9/9`；模板仓库输入、路径白名单、素材处理设置和秘密边界均通过。
- [x] `npm test --silent`：`676/676`；`node --check server.js`、`node --check server/routes/skills.js`、`node --check public/app.js` 通过。
- [x] 4327 正式根页面 HTTP 200，资源缓存为 `0.18.86`；`/api/skills` HTTP 200 返回 12 个技能；8765 `/api/健康` HTTP 200。
- [x] 正式浏览器技能中心显示 12 张卡；模板仓库统一输入区可见、`display:grid`，空输入执行被明确拦截并提示输入要求。
- [x] 正式浏览器右键素材处理卡显示设置/路径/说明/复制 ID 四个真实菜单项；进入设置后开关为开启，源/目标路径回填，路径输入只读且选择/打开按钮可用。
- [x] 正式浏览器素材预览、流量维护、微信聊天提取均完成真实回读；没有执行素材移动/清理、模板真实采集或外部发布。
- [x] 流量转化独立滚动容器实际滚动到 `scrollTop=295.33`；按钮宽度 192/192/100/104，无点赞按钮或点赞文案。

# PACKAGE-086｜Windows 便携版启动闭环（2026-08-15）

- [x] `src/package.json` 的 Electron 打包清单包含 `server/**/*`，并与当前版本 `0.18.86` 输出目录一致。
- [x] 包内存在 `resources/app/server/routes/juguang.js`，不再复现首次启动的 `MODULE_NOT_FOUND`。
- [x] 便携版隔离启动到 `4330`：首页 HTTP 200、资源缓存 `0.18.86`、`/api/skills` HTTP 200、技能数 12；测试进程已退出。
- [x] `npm test`：`676/676`；打包清单回归与技能/路由回归通过。

# SKILLS-084｜素材处理技能与正式浏览器闭环（2026-08-15）

- [x] `node --test public/skills-ui.test.js public/workbench-ui.test.js`：`269/269`。
- [x] `node --test server/routes/skills.test.js`：`7/7`；素材处理预览、结果归一化和秘密不泄漏断言均通过。
- [x] `npm test --silent`：`672/672`；`node --check server.js`、`node --check server/routes/skills.js`、`node --check public/app.js` 通过。
- [x] 4327 正式服务刷新后资源为 `0.18.84`，`/api/skills` HTTP 200 返回 12 个技能；素材处理状态 `overallStatus=ready` 且 `canRun=true`。
- [x] 真实浏览器点击：流量转化维护返回正式 SOP 5、方案 729、服务可达、同步已接入；微信聊天提取返回扫描文件 461、候选索引已刷新、正式写入未写入。
- [x] 真实浏览器运行素材处理“先预览”：完整帖子 0、预计移动 0、空目录 0、残留待确认 2，明确未改动原文件；确认整理按钮保留人工门禁。
- [x] 流量转化页面实际可下滑：独立滚动容器初始 `752/1047`，滚动后 `scrollTop=4546`；前端/后端按钮实测宽度协调，无点赞按钮。
- [x] 设置中心实测“启用素材处理”开关已接入且当前为开启；转换助手 8765 `/api/健康` HTTP 200。

# SKILLS-085｜技能中心通用说明与右键设置（2026-08-15）

- [x] 技能卡、小工具和主链节点包含 `data-tooltip` 及背景、用途、定义、使用、运行、原因六段说明。
- [x] 右键上下文菜单包含技能设置、可配置路径、技能说明和技能 ID；普通目录/账号/生产模式的旧右键动作未被覆盖。
- [x] 素材处理路径设置只允许真实目录，必须包含 `关键词作品`，并拒绝输入/输出目录互相嵌套。
- [x] `GET/POST /api/skills/jianghu-toolbox-material-ingestion/settings` 保留 profile 真源，不返回秘密；路径保存后状态重新读取。
- [x] `node --check server/routes/skills.js`、`node --check public/app.js`、技能中心定向回归 36/36 通过。
- [x] 浏览器指针级实测：技能卡右键菜单、进入设置、路径输入焦点和预览门禁均已验证；本轮未触发提交整理。

# SKILLS-083｜技能中心执行区与触摸端可用性修复（2026-08-15）

- [x] 素材下载和模板仓库执行区不再被 `.skills-layout > .skill-material-runner` 隐藏；技能卡按钮能把焦点带到真实输入区。
- [x] 技能流程轨道统一为“选择能力 → 预检输入 → 执行并回读”；素材下载有连接状态徽标和可访问输入标签。
- [x] 触摸/移动端技能详情无需悬停即可读取；侧栏当前页同步 `aria-current="page"`。
- [x] `node --test public/skills-ui.test.js server/routes/skills.test.js`、Node 语法检查和 `git diff --check` 通过。
- [x] 4327 正式服务冒烟：`/api/skills` 返回 12 个技能，资源缓存为 `0.18.83`；8765 转换助手健康接口返回正常。
- [x] 真实浏览器点击流量转化维护完成：正式 SOP 5、方案 729、服务可达、同步候选待确认；聊天提取完成扫描 461 个文件且未写正式 SOP。

# SKILLS-082｜技能目录快速加载与最终验收（2026-08-15）

- [x] 在线分发五个平台均显示左侧成品库与右侧发布区；抖音、X / 推特、携程旅行使用统一人工网页接力按钮，页面无 `data-ctrip-*` 和 `publishingComingSoon` 残留。
- [x] `VERSION`、Node 包版本、构建输出目录和 HTML 资源缓存均同步到 `0.18.82`。
- [x] `/api/skills` 首次初始化后连续请求约 10–32ms 返回 5 个技能；目录状态不伪报实时连接/运行水位可提交。
- [x] 重启 4327 服务与 Electron 后，浏览器技能入口只有一个可见视图；旧兼容素材区由 CSS 隐藏，不与统一技能卡重复显示。
- [x] 真实点击流量转化维护完成：正式 SOP 5、方案 729、服务可达、同步候选待确认。
- [x] 真实点击微信聊天记录提取完成：扫描文件 461、候选索引已刷新、正式写入未发生。
- [x] 修复模板仓库技能状态互递归导致 `/api/skills` 500，并通过技能路由定向回归 4/4。
- [x] 全量 `npm test` 663/663 通过。

# SKILLS-081｜技能中心接入图文项目工作台（2026-08-15）

- [x] 侧栏只有一个可见“技能”入口，页面只有一个 `skillsView` / `skillsGrid`，没有重复 ID。
- [x] `/api/skills` 返回白名单技能，包含微信聊天记录提取、流量转化模块维护，并保留现有素材下载与客资技能。
- [x] “素材下载”输入框可粘贴一条或多条公开分享文案/链接，执行按钮调用现有 `xhs-dl` / `universal-downloader` V2；默认目录为当前用户 `Downloads`。
- [x] 素材下载任务通过 `/api/skills/material-download/run` 提交并轮询 `/tasks/:id`；结果只回传状态、计数、标题、保存目录和逐条失败原因，不泄露下载器历史文件路径。
- [x] 两个维护技能均有真实异步任务入口、状态轮询、来源说明、流程、安全边界和下一步反馈。
- [x] 技能目录不阻塞实时连接预检；目录状态明确标记“点击运行后实时检查”，不能伪报可预览/可提交。
- [x] 聊天提取只更新候选/索引；流量转化维护只输出服务、SOP、旅程、搜索、方案和工作台快照核验，不自动写正式内容。
- [x] Node 语法检查、技能/工作台定向回归 270/270、全量 `npm test` 662/662 通过。
- [x] 4327 服务冒烟：返回 `C:\Users\z\Downloads` 默认目录；空输入 400；不支持来源真实进入 failed，不伪造成功。
- [x] 重启正式 Electron 与 4327 服务后浏览器点击“技能”；聊天提取返回“扫描文件 461、候选索引已刷新、正式写入：未写入”，维护检查返回“正式 SOP 5、方案 729、服务可达、同步：候选待确认”，没有把候选待确认误判成正式更新。

# PUBLISHING-080｜抖音与 X 默认免 API Key 官方网页接力（2026-08-15）

- [x] 平台目录：抖音/X 默认为 `manualHandoff`，状态为“可手动发布”，不再以 AiToEarn API Key 作为默认门槛。
- [x] 官方入口：抖音 `https://creator.douyin.com/`，X `https://x.com/compose/post`；携程入口保持 `https://we.ctrip.com/`。
- [x] 人工动作：准备发布包、复制内容、打开官方页面、预览和最终发布确认均有真实按钮，未配置 API 也不应禁用准备动作。
- [x] 服务端：人工网页平台的准备与发布门禁已统一；`publish` 不自动提交、不伪造成功。
- [ ] 外部边界：登录、验证码、真实上传和最终发布仍需用户在官方页面完成；本轮不代替用户点击发布。

# GPT-RECOVERY-ACTIONS-077｜恢复/重试/重设/跳过动作收敛（2026-08-15）

- [x] 复现并定位：`WINDOW_STAGE_PENDING` 先经过渲染器安全门，导致原“重试”设置的 `forceUpload` 还没到扩展就被拦截；“继续”又复用了同一发送入口，最终形成按钮可见但动作无效。
- [x] 恢复当前作品改为读取当前对话并校验素材归属；计划、图片、文案和归档阶段分别续接，不会把下一套素材注入旧对话。
- [x] 重试当前作品改为重跑同一作品检查点；重设并重做单独经过确认，只在网页不生成时清理旧边界并重新上传同一套素材；跳过只记录跳过。
- [x] 命令总线传播处理器拒绝结果；UI、助手、语音、自动化共用 `gpt.recover-current`、`gpt.retry`、`gpt.reset-current`、`gpt.skip`，动作审计写入本地有限长度日志。
- [x] 状态栏显示明确下一步；当前对话归属支持完整路径与精确素材文件夹名兜底，避免合法恢复因网页文本不含磁盘绝对路径而被误拒绝。
- [x] 回归：`node --check public/app.js`、`node --check public/workbench-command-bus.js`；GPT 状态/命令/UI 定向回归通过。
- [ ] Electron 桌面实机：在不影响当前生产的窗口中验证恢复、重试、重设确认和跳过四个入口；当前真实队列按要求保持暂停，未点击、未刷新、未重启。

# PUBLISHING-076｜在线发布工作区结构与平台状态恢复（2026-08-15）

- [x] 复现并修复：普通平台标签悬停会触发遮挡工作区的大型延迟说明层；现在仅显式 `data-tooltip` 控件触发详细帮助。
- [x] 复现并修复：平台切换时本地服务短暂重启或多个请求交错会让右侧停在“平台状态读取失败”；现在合并目录请求、隔离过期平台请求、短暂断连自动重试，并提供手动刷新入口。
- [x] 复现并修复：右侧成品摘要可能停留在预览统计，和真实成品源的正文/图片不一致；全文载入后会更新摘要和流程状态。
- [x] 回归断言：在线发布定向 `7/7`；Node 语法检查通过；WHATWG URL 解析替代弃用的 `url.parse`。
- [x] 浏览器验收：4327 页面可在公众号、小红书、抖音、X / 推特、携程旅行之间切换；左侧成品可加载到右侧，快速切换后无平台状态错误。
- [ ] 外部边界：小红书 MCP、AiToEarn 抖音/X 账号仍需用户完成配置/登录后再做真实“检查适配器 → 准备 → 人工确认”联调；本轮没有代替用户提交发布。

# PUBLISHING-073｜统一素材到在线平台发布工作区（2026-08-15）

- [x] 在线分发固定显示左侧现有成品库、右侧平台发布区；左侧不新建素材源，复用公众号草稿作品集/帖子接口。
- [x] 进入页面自动选择第一组真实作品集与第一篇帖子；搜索和泛流量/精准流量/未分类筛选能更新左侧。
- [x] 公众号仍显示图片预览、标题/正文检查和草稿动作；小红书、抖音、X / 推特显示编辑、复制发布包与配置状态；携程显示准备、复制和官方入口。
- [x] 未配置平台的准备发布按钮保持禁用，不调用外部适配器；加载期间显示“正在读取成品库”而不是虚假 0/0 空状态。
- [x] `node --check src/public/app.js`、定向回归 256/256、全量 `npm test` 638/638；4327 浏览器五平台双栏验收、携程准备包和 X 复制发布包通过。
- [ ] 真实小红书 MCP、AiToEarn 抖音/X 账号登录后，再分别执行一次“检查适配器 → 准备 → 人工确认”联调；不把未配置状态当作发布成功。

# CONVERSION-074｜流量转化执行闭环与资料分层（2026-08-15）

- [x] AI 搜索/方案返回不再只显示递归字段；首选回复或策划建议、状态/需求拆解、下一步、缺口、匹配理由和提醒有独立执行区。
- [x] 首选回复与方案摘要可复制；方案仅在存在真实来源路径时显示“打开方案源文件”。
- [x] 用户旅程每个默认决策节点存在“进入对应 SOP”，点击后按角色和关键词进入对应 SOP 环节。
- [x] 同步中心默认显示业务使用口径，维护者详情可展开；候选仍保持 formalWrite:false 边界。
- [x] node --check src/public/app.js、node --check src/server.js 和工作台定向回归已通过。
- [x] 桌面验收：重启 4327 后已在正式页面执行“AI 智能建议”“AI 策划匹配”“进入对应 SOP”“打开方案源文件”，并展开同步详情；确认结果卡、方案动作、跳转和正式/候选口径均可见。

# GPT-PATROL-RECOVERY｜巡检续接与普通生产恢复（2026-08-15）

- [x] `patrolLedgerKey` 的 `::material:长度:哈希` 作用域不会再被当成导航 URL。
- [x] 恢复优先使用实时 GPT 对话 URL，并兼容普通对话与自定义 GPT 对话路径。
- [x] `desktop:gpt-patrol-continue` 对无效目标返回结构化错误，继续/重试不会再出现 `Error invoking remote method`。
- [x] 主进程、渲染器语法检查与巡检相关定向回归 3/3 通过。
- [ ] Electron 桌面实机：安全重载后点击当前暂停任务的“继续/重试”，确认从原检查点恢复，不重复上传。

# MATERIAL-069｜江湖工具箱下载后素材区维护（2026-08-15）

- [x] 图文工作台素材区带专属说明，明确江湖工具箱只下载、整理技能按20字入库、刷新后使用。
- [x] 页面帮助包含“素材区怎么维护”，说明完整帖、评论/点赞、标签、整包移动和残留边界。
- [x] 静态回归断言保护素材区说明，不允许以后只剩无法理解的“素材区”标题。
- [ ] 新下载完整图文帖子出现后，执行一次“预览 → 入库 → 素材区刷新”桌面验收，确认标签筛选与批次数量一致。

# PLATFORM-068｜统一在线平台分发适配层并入主工作台（2026-08-15）

- [x] 4327 主工作台源码已包含 lib/platform-publishing.js 与 server/routes/platform-publishing.js，不是旁边的临时分支。
- [x] 在线平台页显示公众号、小红书、抖音、X / 推特、携程旅行五个平台，状态由本地目录接口返回。
- [x] 携程页面可准备标题、正文和图片清单，并打开官方内容中心；不会自动提交发布。
- [x] node --check、平台路由回归、携程手动交接回归和主工作台 UI 回归通过（264/264）。
- [ ] 4327 重启后在浏览器中打开在线分发 → 携程旅行，确认面板和已登录的携程官方页面；最终发布仍由用户手动确认。

# UI-THEME-066｜深浅主题与右侧 GPT 网页同步（2026-08-15）

# DISTRIBUTION-068｜后台分类库存补全（2026-08-15）

- [x] 后台监测只使用 `--status-json --status-background`，不再与 `--status-udp-only` 冲突。
- [x] 后台监测使用 `--status-background`：UDP 无回复时快速返回，不触发全网回退扫描。
- [x] `--status-fast` 在 UDP 发现设备后补读 `/v2/info`，保留 `workCounts.conversion/traffic`；`--status-udp-only` 仍可单独作为只读诊断命令。
- [x] 实机探针：红米 13 返回 `conversion=7`、`traffic=0`；VIVO 当前 `/v2/info` 超时，系统不把总库存误当精准库存。
- [x] 工作台定向回归 289/289；未执行真实发送，未改动生产队列。
- [ ] VIVO 接收端稳定响应 `/v2/info` 后，再验收一次自动分发“库存判断 → 候选作品 → 发送 → 记录”闭环。

- [x] 主工作台主题切换同时更新根文档、`body` 的 `data-theme` 和 `color-scheme`。
- [x] 右侧 GPT 在 `dom-ready` 时重放当前主题，并在网页根节点主题属性变化后重新应用。
- [x] `node --check src/public/app.js`、`node --check src/desktop/main.js`、主题相关静态回归 2/2 和全量测试 616/616 通过。
- [x] 本地 4327 页面循环验证 neo、glass、midnight、midnight-glass、neo，浅色/深色状态与视觉字体颜色一致。
- [ ] Electron 桌面实机验收：在不打断生产队列的前提下安全重载右侧 GPT 窗口，切换深浅主题并确认网页背景、字体和原生控件同步。

# CONVERSION-066｜小红书话术版本与复制体验（2026-08-15）

- [x] 用户旅程决策地图显示“树状图是什么意思？”并说明入口、判断点、继续分支和补信息分支。
- [x] F01Q04 保留企业号明确版，新增 F01Q04A2 克制版，两个版本都有适用备注并可复制。
- [x] 带备注的多回复默认展开；复制按钮统一右对齐。
- [x] 转化 SOP 不再渲染点赞按钮，不再绑定点赞事件。
- [x] Node 语法检查、工作台回归和正式 SOP 运行数据校验通过。
- [ ] 桌面验收：刷新流量转化 → 转化 SOP，进入 F01，确认小红书问题显示两种话术、备注和右侧复制按钮。

# CONVERSION-065｜决策树执行进度（2026-08-15）

- [x] 进度卡显示当前节点、已完成数量、百分比和可访问的 progressbar。
- [x] “完成并进入下一步”真实写入 tb-conversion-journey-progress-v1，优先按 yes.target 推进并自动定位下一节点。
- [x] “从这里开始/重做这一步”会切换当前节点并清理其后的完成标记；“重新开始”会清空本轮进度。
- [x] 未知或未来新增节点会在读取 localStorage 时被过滤，不阻塞现有决策树。
- [x] Node 语法检查与 public/workbench-ui.test.js 定向回归通过。
- [ ] 桌面验收：流量转化 → 用户旅程中依次点击完成、刷新、重做、重新开始，确认卡片状态、滚动定位和窄屏布局。

# MOMENTS-UI-062｜朋友圈自动发帖面板（2026-08-14）

- [x] 0.18.72 状态与路径收口：面板读取当天 `publisherState` 并锁定重复准备；作品库保留配置路径，`doctor-state` 可同时显示配置路径和 junction 真实目标。
- [x] 0.18.72 中断恢复保护：`recover-preparing` 需精确 work_id、人工原因和 `--confirm-interrupted`；已准备/已确认记录拒绝恢复。
- [x] 0.18.70 旧 pyweixin UIA 发布回退已禁用；遇到 `mmui::MainWindow` 返回 `WECHAT_LEGACY_UIA_DISABLED`，不调用 `Navigator.open_moments()`，不启动讲述人。
- [x] 独立导航：存在 `data-tab="moments"` 和 `momentsView`，不替换公众号在线分发页。
- [x] 真实素材读取：`/api/moments/library` 返回本地 ready 作品、封面 URL、图片列表、`content.txt`、日期、标签和状态。
- [x] 筛选与预览：分类、标签、今年/去年的今天/全部历史、搜索；预览最多九张图和原始文案。
- [x] 发送边界：请求指定 workId，调用 Python `--live --work-id`；服务端不提供自动发布确认接口，Python 适配器不点击最终发表。
- [x] 状态保护：FAILED/非 QUEUED 作品不可从面板发送；当天已有状态时由 publisher state gate 阻止继续准备。
- [x] 自动回归：Node 语法检查、朋友圈面板契约/纯函数测试、朋友圈 Python 定向回归 25/25、HTTP library API 144 条；全量工作台回归随版本验收更新。
- [x] 历史实机回归：微信 4.1.12.55 已验证 9 张图片全部进入九宫格，原文案 1067 字符回读一致，最终发表按钮未点击。
- [ ] 升级后登录微信实机回归：再次确认当前渲染画布路径仍能完整进入九宫格并回读文案；不点击最终发表作为自动化验收边界。
- [ ] 人工闭环：手动点击发表后运行 `python -m moments_publisher --library "D:\朋友圈weflow" mark-published`，确认 ready → used 和当天记录。

# RELIABILITY-062｜自动分发设备诊断身份（2026-08-14）

- [x] 自动分发跳过/等待消息可带电脑备注、设备名称和型号；没有身份时保持旧文案，兼容历史日志。
- [x] 真实设备身份继续写入 `deviceName`、`deviceModel`、`deviceNote`、`deviceDisplayName`，UI 可显示具体目标而非“全局设备”。
- [x] 定向回归覆盖有身份与无身份两种文案。

# UI-063｜当前窗口标签语义（2026-08-15）

- [x] 内容制作发送区只显示当前窗口名称，不重复渲染“账号”前缀。
- [x] 账号绑定、窗口切换、队列启动/停止行为保持原样。
- [x] 静态工作台回归覆盖标签语义。

# DISTRIBUTION-064｜UDP-only 状态探测退出边界（2026-08-15）

- [x] `send_to_device.py --status-udp-only` 在无设备时仍返回 0，不要求发送用的 `--source/--device`。
- [x] UDP-only 仍不回退全网 HTTP 探测；发现结果继续输出结构化状态供后台轮询消费。
- [x] Python 技能回归：33/33；真实局域网本轮未发现在线设备，未伪造自动分发成功。

# RELIABILITY-061｜首屏快照、GPT 初始化与后台设备发现（2026-08-14）

- [x] 首屏 `/api/dashboard?lite=1` 不加载完整设备/分发明细；进入相关页面后再按需加载。
- [x] 分发页面轮询使用 `/api/distribution/live`，相同 dashboard 请求由 in-flight promise 合并。
- [x] 后台设备扫描显式带 `--status-udp-only`；手动完整刷新入口仍保留 HTTP fallback。
- [x] GPT 首次页面初始化在 `did-finish-load` 与 `loadURL()` 竞态下只执行一次，避免重复主题观察器。
- [x] 全量回归：610/610；定向回归：250/250。
- [x] 局域网实测：UDP-only 曾发现到 1 台 vivo（192.168.0.103:45833），没有触发全网 HTTP 回退；后台路径不再等待 HTTP 增强，手动完整刷新仍保留增强。
- [ ] 桌面实体验收：仍需在该真实在线手机上确认库存上报和自动分发首轮闭环；本轮不伪造“已发送成功”。

# UI-CONVERSION-062｜旅程高级设置（2026-08-14）

- [x] 右上角设置：用户旅程页存在可打开/关闭的高级设置面板，不遮挡主路径。
- [x] 真实生效：密度、四步执行法、分支解释、直接话术和完整 SOP 默认展开均有对应渲染变化。
- [x] 持久化：设置写入当前电脑 localStorage，刷新后保持；恢复按钮回到开箱默认。
- [x] 适用边界：默认空画板和已有旅程画板都能打开设置；不改变业务数据和正式知识库。
- [x] 自动回归：`node --check src/public/app.js`、`node --check src/server.js`、`node --test --test-concurrency=1 src/public/workbench-ui.test.js`（244/244）。
- [ ] 实机验收：刷新到 0.18.60，进入用户旅程，点击设置逐项切换并刷新，确认每项都保持生效；最后点击“恢复开箱默认”。

# UI-CONVERSION-061｜旅程节点执行化（2026-08-14）

- [x] 每个节点展示“现在做”和“做完算”，不用先展开 SOP 才知道动作标准。
- [x] 每个节点展示“直接照着说”并可复制，复制后仍使用现有旅程话术提示。
- [x] 每个节点展示“卡住时”处理方式，避免客户不确定时乱推进。
- [x] 顶部四步法明确操作顺序：看信号 → 做动作 → 照着说 → 看完成。
- [x] 响应式：执行区和话术区在 620px 下自动堆叠，地图仍保持可滚动。
- [x] 自动回归：`node --check src/public/app.js`、`node --test --test-concurrency=1 src/public/workbench-ui.test.js`（243/243）。
- [ ] 新人验收：找一个没有看过 SOP 的人，只看地图完成第 01-03 节点；记录仍需要解释的词，再继续收敛文案。

# UI-CONVERSION-060｜决策树数据模型可扩展化（2026-08-14）

- [x] 树入口：存在明确 `root`，节点通过唯一 `id` 管理，不依赖数组位置。
- [x] 分支连接：`yes/no` 分支带 `target`，地图能显示“下一节点”或“结束/待跟进”。
- [x] 单一数据源：作战地图和完整 SOP 均由同一棵树遍历生成。
- [x] 向后可读：现有 01-08 节点标题、信号、话术、判断和业务路径保持不变。
- [x] 自动回归：`node --check src/public/app.js`、`node --test --test-concurrency=1 src/public/workbench-ui.test.js`（243/243）。
- [ ] 扩展验收：在树数据中新增一个测试节点并将某个分支 `target` 指向它，刷新工作台确认地图和完整 SOP 同时出现该节点；验收后再决定是否保留业务节点。

# UI-CONVERSION-059｜用户旅程决策树作战地图（2026-08-14）

- [x] 首屏路径：默认旅程显示 8 个纵向节点，并以中轴、节点编号和向下箭头表达顺序。
- [x] 分支可读：每个节点同时显示进入信号、绿色“是/继续推进”和琥珀色“否/先停一下”，下一站单独突出。
- [x] 信息保留：展开“完整 SOP 话术与判断说明”后，原有复制话术、判断方法和详细分支仍可用。
- [x] 响应式：980px 下图例换行，620px 下是/否分支堆叠，旅程工作区仍可纵向滚动。
- [x] 主题：明亮、midnight、midnight-glass 三种主题的地图卡片和折叠区均有对应表面与文字对比。
- [x] 自动回归：`node --check src/public/app.js`、`node --check src/server.js`、`node --test --test-concurrency=1 public/workbench-ui.test.js`（243/243）。
- [ ] 桌面验收：刷新工作台到 0.18.57，进入流量转化 → 用户旅程，确认先看到作战地图；点击“展开完整 SOP”确认复制按钮仍可用；缩窄窗口确认分支堆叠且页面可下滑。

# UI-CONVERSION-058｜同步状态、候选边界与旅程分流（2026-08-14）

- [x] 正式/候选边界：页面显示正式 SOP 与待审核增量分开；`formalWrite:false` 不进入正式更新口径。
- [x] 同步元数据：服务端只读报告文件，展示正式更新时间、候选消息数、变化会话数、API 状态和最近运行时间；找不到运行目录时降级为等待同步状态。
- [x] 旅程分流：显示“公域已确认人数 → 发对应帖子图 → 直接拉群”，并保留客户想先聊/信息不清时的轻互动备用路径。
- [x] 空画板边界：0 节点/0 连线时明确说明展示的是默认流程，不冒充可编辑画板已完成。
- [x] 自动回归：`node --check src/public/app.js`、`node --check src/server.js`、`node --test --test-concurrency=1 public/workbench-ui.test.js`（243/243）。
- [ ] 桌面验收：重启或刷新工作台到 0.18.56，进入流量转化，确认同步中心显示“待审核增量”、6,789 条待审核消息和 API 暂缓；切换“用户旅程”确认分流卡、空画板说明、窄屏折叠和长内容滚动。

# GPT-RECOVERY-058｜单账号归档恢复、额度覆盖与账号绑定（2026-08-14）

- [x] 复现条件：已生成图片和文案的 GPT 会话标题为空；队列显示 `production-title-required`，当前账号额度实际为上传 0 / 生图 0。
- [x] 根因确认：桌面层传递的 `allowUntitledRecovery` 在扩展消息转发处丢失；归档检查先于任务账号绑定；过期 `waiting-quota` 运行态没有有效 `nextProbeAt` 仍会遮盖归档状态。
- [x] 修复：补齐扩展参数；恢复/重试前绑定账号；“继续/重试”仅覆盖本地安全线；过期额度运行态不再覆盖归档错误。
- [x] 自动回归：`node --check src/public/app.js`、`node --check src/integrations/gpt-production-extension/sidebar.js`、状态策略和定向 UI 测试。
- [ ] 桌面验收：不重启应用，刷新工作台后在 `4-embrace.ping` 点击一次“继续尝试”或“重试”，确认当前作品先完成归档、错误消失，再按队列进入下一套；若 GPT 网页真实限额，确认显示真实限额而不是误报标题错误。

# UI-CONVERSION-057｜流量转化数据口径与最短路径（2026-08-14）

- [x] 快照对账：当前已入库统计为有效会话 450、总消息数 19,254、客户转化群 179、前端私聊会话 208、真实问答 3,523、可检索问答 3,520、方案索引 729。
- [x] 关键业务细节：F04/F06 含“公域已确认人数 → 加微信 → 发送对应帖子图 → 直接拉群”，并保留信息不清时的轻量互动备用路径。
- [x] 代码回归：统计渲染优先使用 `总消息数`、`客户转化群`、`前端私聊会话`，旧快照字段缺失时回退扫描统计。
- [x] 桌面验收：运行中的 `127.0.0.1:4327` 已刷新到 0.18.54；顶部实际显示 `3,520 / 450 / 19,254 / 179 / 208`，SOP 实测包含“帖子图后直接拉群”和轻量互动备用路径。

# UI-CONVERSION-056｜流量转化滚动与模块宽度统一（2026-08-14）

- [x] 复现条件：进入流量转化并打开长内容模块，页面无法继续下滑；切换后续模块时内容区宽度比首个模块窄。
- [x] 根因确认：页面外壳没有稳定的 100% 高度链路，内部工作区滚动层没有可靠的受限高度；搜索、SOP、方案、旅程使用了不同根宽度和侧栏约束。
- [x] 修复：补齐页面/外壳/主内容高度，统一由 `.conversion-workspace` 纵向滚动；四个模块根节点全宽，SOP 侧栏统一 220px，方案页移除 920px 最大宽度。
- [x] 自动回归：`node --check src/public/app.js`；`public/workbench-ui.test.js` 242/242；`git diff --check` 通过。
- [x] 桌面验收：已在 1280×720 运行页面打开四个模块；搜索页实测 `scrollTop=278` 到底，SOP/方案/旅程根节点均约 1046px、工作区 1056px，左右边界一致。

# UI-HELP-055｜全界面使用说明入口与内容（2026-08-14）

- [x] 内容制作、提示词管理、流量转化、聚光运营、自动化工作流均有统一 `?` 入口和详细说明。
- [x] 文件传输、作品仓库、在线分发、设置中心原有帮助入口继续保留。
- [x] 说明覆盖背景、用途、对象/范围、怎么用、如何运行、结果标准与常见排错。
- [x] 长说明继续使用可滚动系统弹窗，不改变业务页面滚动和按钮行为。
- [x] 定向回归 263/263；语法检查通过；`git diff --check` 通过。
- [ ] 桌面验收：刷新/重启 0.18.52 后逐页点击 `?`，检查深色主题、窄窗口、滚动、关闭和页面切换。

# 团建工作台问题回归库

## CONVERSION-CORE-054｜流量转化内置文字知识与后台资料同步（2026-08-14）

- [x] 流量转化仍是主工作台同源页面，不新增独立知识库入口或 iframe。
- [x] 首次进入先显示内置前端运营/后端转化 SOP 与可复制话术；不等待外部服务启动。
- [x] 外部历史问答、方案索引和旅程快照在后台同步；同步失败时保留内置文字内容，不显示“模块暂时不可用/重新加载”。
- [x] PDF/PPT 等外部资料保留路径读取边界，不复制大文件进入主工作台。
- [x] 自动回归：`node --test --test-concurrency=1 public/material-workspace.test.js public/workbench-ui.test.js`；本轮新增断言通过，剩余设置页 `checkAppUpdateBtn` 为既有失败。
- [ ] 桌面验收：刷新/重启 0.18.46，进入流量转化，确认未连接/慢服务时也能立即打开“转化 SOP”，复制一条内置话术；服务恢复后确认状态变为已同步，页面不闪退、不清空内容。

## UI-PUBLISHING-048｜在线分发页头与平台接入状态（2026-08-14）

- [x] 在线分发页头压缩，平台栏与真实公众号工作区之间保持紧凑间距。
- [x] 四个平台按钮使用 `role="tab"` 与 `aria-selected`，当前选中项有明确视觉/键盘焦点反馈。
- [x] 公众号显示“已接入”；小红书、抖音、携程旅行只显示“待接入”，不调用假登录、假上传或假发布。
- [x] 切换到携程后能看到真实待接入说明，再切回公众号，作品集与草稿检查台不丢失。
- [x] 自动回归：`public/workbench-ui.test.js` 238/238；语法检查与 `git diff --check` 通过。
- [ ] 桌面验收：刷新/重启 0.18.45，确认页头首屏占用收紧、四个 tab 可点击/键盘切换；在已有登录前提下再单独验收公众号草稿保存。携程仍应保持待接入，不得出现假发布按钮。

## UI-WORKS-047｜作品仓库页头与本地目录打开入口（2026-08-14）

- [x] 作品仓库页头压缩，移除重复的顶部目录打开按钮。
- [x] 本地目录地址栏新增“打开”按钮，位于“选择”前，使用当前配置路径调用桌面打开接口。
- [x] 未配置路径/打开失败有明确提示，且不修改原始作品文件、扫描状态或分发状态。
- [x] 自动回归：`public/workbench-ui.test.js` 236/236；语法检查与 `git diff --check` 通过。
- [ ] 桌面验收：刷新/重启 0.18.44，进入作品仓库，确认页头占用减少；点击“打开”能打开地址栏中的本地目录，点击“选择”仍能选目录并回填。

## UI-GPT-046｜模板分类、链接动作与展开保持（2026-08-14）

- [x] 模板区移除“模板来源”，保留模板分类筛选；本地模板和带在线链接的模板按名称/模板 ID合并。
- [x] 模板行提供链接按钮：复制链接、发送到当前 GPT 对话并访问；“上传模板”仍保留本地附件上传及链接模板快捷动作。
- [x] 展开模板/素材面板在悬停、聚焦和操作期间保持展开，离开十秒后自动收起，手动按钮可立即折叠。
- [x] 自动回归：`public/workbench-ui.test.js` 235/235；语法检查和 `git diff --check` 通过。
- [ ] 桌面验收：刷新/重启 0.18.43，添加一个在线模板，点击链接按钮确认当前 GPT 先收到链接再跳转；验证鼠标停留/操作不会提前收起。

## DISTRIBUTION-LOG-053｜分发日志人话化与自动刷新（2026-08-14）

- [x] 复现条件：自动分发完成后查看“操作记录”，只能看到事件名/百分比/内部状态；自动记录还可能没有计入顶部记录数，页面需要手动刷新。
- [x] 根因确认：事件归一化直接把 `message`、`event`、ISO 时间和任务信息拼进单行；后台轮询只检查素材缓存时间，没有更新 `dashboard.distribution`。
- [x] 修复：统一生成结论、结果徽章和可展开的人话明细；明细保留源/目标路径、文件数、大小、库存、阈值、重试和技术追踪号；快照变化时只读更新分发历史。
- [x] 自动回归：`public/distribution-ui.test.js` 覆盖 VIVO 发送、微信公众号移动、自动完成、北京时间和文件大小；`public/workbench-ui.test.js` 覆盖人话字段、详情展开、自动记录计数和后台快照刷新。
- [ ] 桌面验收：刷新/重启工作台后打开“文件传输 → 操作记录”，确认先看到人话结论；展开一条记录能看到明细；让自动分发完成一次后不手动刷新，确认记录数和记录内容自动更新，并确认没有新增第二次发送。

## UI-GPT-045｜内容制作素材标签下拉定位（2026-08-14）

- [x] 根因确认：内容制作素材卡/筛选条的变换上下文会让固定菜单偏离触发标签。
- [x] 修复：打开筛选时关闭该卡片和筛选条的变换，菜单按触发标签的视口位置展开。
- [x] 自动回归：`public/workbench-ui.test.js` 断言内容制作筛选打开态保持 `transform: none !important`。
- [ ] 桌面验收：刷新内容制作页，依次点击内容类型、地点、时间等标签，确认每个菜单都紧贴按钮下方；再切到作品仓库确认原筛选表现不变。

## TRANSFER-ROADMAP-052｜LAN V2、设备中转与远程 relay 分层（2026-08-14）

- [x] 已确认当前主链为 UDP 发现 + HTTP/TCP 上传 + SHA-256 + `commit`。
- [x] 已确认 Android 的 relay 仅是局域网设备间中转骨架，不是公网远程中继。
- [x] 已写明新传输必须能力协商、可回退 `lan-v2`，不得绕过库存未知/授权/去重保护。
- [ ] 记录真实 LAN V2 基线：发现耗时、速度、失败率、断线恢复、大文件。
- [ ] `peer-forward` 完成多跳去重、过期、失败恢复和日志真机回归。
- [ ] 只有基线证明有瓶颈后，才做 `direct-peer` 实验；未通过不得替换默认链路。
- [ ] 跨网 `relay` 另立服务并完成加密、令牌、清理、配额、审计和手机兼容验收。

## CROSS-PLATFORM-051｜手机端跨品牌兼容与升级不得回归（2026-08-14）

- [x] 产品约束已写入 `docs/DEVICE-COMPATIBILITY-MATRIX.md`、设计说明和架构说明；电脑端控制台与手机接收端边界明确。
- [x] 通用内核与项目 profile 分界明确：发现、库存、传输、去重、重试、恢复通用；精准/泛流量、目录、内容类型和阈值可配置。
- [x] 手动优先规则明确：新设备/新包先手动闭环，再允许自动补货；库存未知不得误推。
- [x] Android 候选 `0.6.19 / versionCode 57` 已构建、测试、Lint，并传到当前 VIVO；尚未完成手机系统安装确认。
- [ ] 小米/红米：用当前发布接收端完成手动发送、分类库存、自动补货、断线重连。
- [ ] VIVO：安装候选包并重新打开接收端，确认 `/v2/info` 返回 `workCounts`，再完成手动→自动闭环。
- [ ] 华为：至少完成一次真实手动发送和一次库存未知保护回归。
- [ ] Apple：完成 iOS 接收端/适配层后，使用实机验证；在此之前保持“目标支持/待验证”。
- [ ] 每次手机端版本发布前保存旧包、校验签名链和 SHA256，并记录各设备族结果；未验证项不得标记为完成。


## TRANSFER-ALIAS-046：业务设备别名解析到在线传输目标（2026-08-12）
- [x] 复现：界面发送“6号”时，底层 `send_to_device.py` 只按手机在线发现名称/型号匹配，日志报 `设备匹配失败`，任务在上传前失败。
- [x] 修复：服务端在手动文件传输、作品包分发、快捷补货和取消遗留任务前统一解析 `transportDevice`；前端优先提交实时名称/型号。
- [x] 回归：别名 `6号` + 型号 `Xiaomi Redmi Note 8` 可解析为当前在线 `WRedmi Note 8（A2）`；原文件与 GPT 登录数据不变。
- [ ] 桌面验收：刷新/重启后选择“文件传输 → 在线设备 → 上传其他”，确认真实文件或文件夹任务进入进度并完成。

## TRANSFER-PICKER-045：桌面上传入口使用原生选择器（2026-08-12）
- [x] 文件夹入口调用 `window.desktopDialogs.pickFolder`，文件入口调用 `window.desktopDialogs.pickFile`。
- [x] Electron 主进程注册 `desktop:pick-folder` 与 `desktop:pick-file`，取消选择返回空路径且不创建任务。
- [x] 浏览器调试版仍保留 `/api/pick-folder` 与 `/api/pick-file` 回退。
- [x] 自动化回归覆盖原生优先级与回退存在性。
- [ ] 桌面验收：打开“文件传输 → 在线设备 → 上传其他”，分别选真实文件和文件夹，确认任务进入实时进度并完成。

## UI-GPT-044：分步工作流与提示词入口可见（2026-08-12）
- [x] 自动生产设置的工作流编辑器默认展开，首屏可看到当前模式的完整步骤摘要。
- [x] 上传计划、确认扣 1、生图、文案、下载、归档等文本步骤仍提供“查看 / 编辑”，等待/检测/重试参数可调整。
- [x] 切换生产模式后编辑器读取对应独立 profile，不串用其他模式配置。
- [x] 自动回归：覆盖默认展开、模式专属标题/摘要、进入 `gptAuto` 设置时调用展开逻辑。
- [ ] 桌面验收：刷新工作台，打开设置中心 → 自动生产设置，在“工作流与提示词（本模式专属）”中分别切换模式并编辑一条提示词，确认保存后再次打开仍保留。

## UI-ASSISTANT-043：小猫失焦闪退与设置空白（2026-08-12）
- [x] 复现条件：拖拽小猫或透明鼠标事件交接时，原生子窗口短暂失焦。
- [x] 根因：子窗口 `blur` 监听直接隐藏；每条通知重复设置父窗口/置顶模式。
- [x] 修复：由主工作台 `blur` 统一处理离开应用；通知仅在窗口模式设置变化时重应用。
- [x] 自动回归：覆盖无子窗口 blur 隐藏监听、模式变化守卫和设置全局子组双列布局。
- [ ] 桌面验收：在内容制作、设置中心之间切换并拖拽小猫，确认小猫不闪退；打开自动生产设置确认全局设置不再出现大块空白。

## UI-IA-042：工作区命名收口（2026-08-12）
- [x] 左侧导航、页面标题和设置分类统一显示“作品仓库”“文件传输”“在线分发”。
- [x] 内部 `works`、`distribution`、`publishing` tab 值保持不变，业务路由与数据逻辑不受影响。

## UI-TEMPLATE-041：模板来源合并筛选（2026-08-12）
- [x] 模板区使用单一来源筛选，可选全部、本地模板、在线模板。
- [x] 在线模板添加入口和本地/在线上传逻辑继续保留。

## UI-TAGS-040：筛选下拉层底部空间（2026-08-12）
- [x] 下拉菜单保持向下展开，靠近底部时限制自身高度而不是翻转方向。

## UI-TAGS-039：筛选下拉层级与方向（2026-08-12）
- [x] 标签筛选菜单使用视口级固定层，脱离素材卡片的 overflow 裁剪。
- [x] 菜单从触发按钮下方展开，并在窗口边界内自动收缩。

## UI-TAGS-038：筛选下拉层优先向上（2026-08-12）
- [x] 有足够上方空间时，标签筛选菜单在顶部控件层向上展开。
- [x] 靠近窗口顶部时，菜单自动回退到下方展开。

## UI-TAGS-037：筛选按钮窄面板换行（2026-08-12）
- [x] 标签筛选栏支持多行换行，按钮不被压缩或截断。

## UI-THEME-036：在线平台分发深色控件（2026-08-12）
- [x] `midnight` / `midnight-glass` 覆盖公众号原生编辑器表面、图片检查卡、标题和正文输入框。
- [x] 增加静态回归断言，防止在线平台页面重新引入白色控件底。

## UI-TAGS-035｜筛选栏去除重复提示（2026-08-12）

- [x] 素材、作品、设备筛选栏不再显示重复的“标签筛选”文字。
- [x] 内容类型、团建游戏、地点、时间等分组按钮继续可见并可操作。
- [x] 自动测试覆盖共用渲染函数无多余提示节点。

## UI-THEME-034｜深色导航图标轮廓（2026-08-12）

- [x] 深色主题下六个左侧导航图标均不使用实体白色填充块。
- [x] 流量转化选中态保留清晰的青色背景、左侧指示线和轮廓图标。
- [x] 自动测试覆盖 `midnight` / `midnight-glass` 的 `fill: none` 规则。

## UI-TAGS-033｜标签下拉不得被卡片裁剪（2026-08-12）

- [x] 素材、作品和设备标签下拉打开后显示在列表卡片之上，而不是藏在底部。
- [x] 下方空间不足时向上展开，右边空间不足时右对齐，长列表内部可滚动。
- [x] 同一时间最多打开一个标签组。
- [x] 自动回归覆盖祖先 overflow 释放、弹层方向定位与 toggle 事件。
- [ ] 桌面验收：依次点击内容类型、团建游戏、地点、时间等下拉，确认选项完整可见且不遮挡失控。

## UI-THEME-032｜全工作台深色主题（2026-08-12）

- [x] `midnight` 与 `midnight-glass` 下，六个一级界面的页面表面均使用全局深色语义。
- [x] 作品库来源栏、阶段栏、作品卡、状态徽章和标签不再显示大片浅灰色。
- [x] 流量转化统计、身份、输入、结果、历史问答、SOP、方案和旅程不再强制使用浅色拟态底板。
- [x] 自动测试断言两套深色主题均覆盖全部一级工作区和流量转化关键卡片。
- [ ] 桌面验收：重启后逐页查看内容制作、作品库、设备分发、在线平台分发、流量转化与设置中心，确认无遗留刺眼浅色块。

## UI-GPT-031｜深色标签、地址栏加载反馈与发送区去重（2026-08-12）

- [x] 复现条件：切到午夜深色主题，素材标签触发器与菜单沿用浅色半透明背景，文字与按钮几乎不可见。
- [x] 根因：标签组件新增在样式文件末尾，未纳入既有深色主题覆盖。
- [x] 修复：两套深色主题覆盖标签栏、触发器、菜单、普通选项与已选选项。
- [x] 加载反馈：原生 GPT 的 `did-start-loading / did-finish-load / did-fail-load` 经 preload 传至 renderer，地址栏显示和收起真实状态进度。
- [x] 控件减法：删除重复模式、状态、下一步概览；保留当前账号、模式、主动作、状态栏和恢复动作。
- [x] 自动测试：主题选择器、加载事件链、DOM 进度条和去重断言已加入 `public/workbench-ui.test.js`。
- [x] 全量自动回归 533/533；开发版重启后 4327 返回 HTTP 200。
- [x] 发送控制减法：移除可见的“生产模式”冗余标签；暂停收尾时主按钮独占“继续当前流程”；运行中隐藏必然被拒绝的“跳过当前”；停止后有可恢复队列时隐藏重复“启动”。
- [ ] 桌面验收：深色主题逐个展开标签；点击刷新观察进度条开始与完成；发送区在暂停、运行、收尾恢复和停止恢复状态下均只有一个主继续/启动入口。

## WECHAT-LAYOUT-030｜公众号分发采用左右工作区（2026-08-11）

- [x] 左侧集中真实路径、发布入口、账号状态、分类、作品集和帖子列表。
- [x] 右侧原位切换草稿检查台与公众号原生网页，不弹出平行项目。
- [x] 宽屏固定 `320–390px` 控制栏，右侧填满剩余空间并独立滚动。
- [x] 900px 以下自动单列；实测 517px 视口无横向溢出。
- [x] 公众号登录分区、草稿队列、去重与保存实现未改动。

## DISTRIBUTION-INVENTORY-028｜分类库存未知时不得静默或误推（2026-08-11）

- [x] 自动分发开关、自动发现、分类、阈值、候选作品集和每轮发送数均从真实设置读取。
- [x] `workCounts=null` 时不把总作品数冒充精准流量类或泛流量类库存。
- [x] 界面显示可执行中文原因，并明确需要升级接收端后重新连接。
- [x] 同时覆盖库存充足、未信任设备、无候选作品和发送数量为零的可读提示。
- [x] 完整自动回归 487/487 通过。
- [ ] 手机安装 Android 0.6.17 后确认 `/v2/info` 返回分类 `workCounts`，再真实验证低于阈值自动补 1 个作品集、成功记账与去重。
- 当前外部阻塞：旧接收端在应用内覆盖更新提交阶段误报“安装包没有签名”，任务已安全取消；手机作品未被修改。

## WECHAT-SIDEBAR-029｜公众号分发应有独立一级入口（2026-08-11）

- [x] 左侧导航显示“公众号分发”，内容分发页不再混入公众号内部标签。
- [x] 独立入口继续复用原有公众号草稿队列、登录分区和原生网页层。
- [x] 离开公众号工作区时原生层隐藏，不覆盖手机分发或其他页面。
- [x] 工作区恢复支持 `wechat` 标签，自动回归覆盖导航、面板归属和原生层可见边界。

## WECHAT-VIEW-027｜公众号原生编辑器不得覆盖工作台（2026-08-11）

- [x] 草稿工作区高度限制在 520–760px，作品集数量不能把原生编辑器撑到上千像素。
- [x] 原生网页层使用宿主与可视视口的交集，`x/y` 不为负，宽高不超过当前可见区域。
- [x] 滚动、缩放、返回检查台或切换分发标签时，原生网页层同步边界或隐藏。
- [x] 测试只验证公众号登录页和界面切换，不保存、不发布草稿。

## DESKTOP-RESTART-026｜自动测试重启不应弹人工确认（2026-08-11）

- [x] 自动化、Codex 或开发测试调用桌面重启桥时默认 `interactive=false`，即使生产状态活跃也直接安排重启。
- [x] 托盘“重启工作台”显式使用 `interactive=true`；人工点击且生产活跃时仍显示“取消 / 重启”。
- [x] preload、IPC 与主进程显式传递 `source / interactive`，不再用笼统的 `renderer` 来源猜测交互语义。
- [x] 静态回归同时断言开发版/自动化免确认、正式安装版人工入口保留确认，并禁止旧 `restartApp("renderer")` 回归。
- 数据安全边界：不清理登录分区、Cookie、生产队列、素材或成品；只调整重启确认策略。

## GPT-COPY-015｜成品文案禁止素材来源旁白（2026-08-10）

- [x] 默认文案提示词明确禁止提及 TXT、图片、附件、素材、参考文案、出图计划、提示词、AI、模型和读取过程。
- [x] 用户自定义文案提示词保留原文，同时自动追加不可绕过的成品质量底线。
- [x] 共享检测器拦截 5 类典型来源旁白，并放行 5 类自然成品表达。
- [x] 首次命中后只在当前对话完整重写一次；第二次仍命中则抛出 `COPY_META_NARRATION`。
- [x] TXT 保存、图片下载、打包与归档之前均执行硬检查；不合格时不落盘、不计次、不前进队列。
- [x] 普通主链、恢复链和单账号多对话巡检共用检测规则，没有建立平行状态机。
- [x] 针对性回归 `222/222`、完整自动回归 `430/430`、语法与差异检查通过。
- [ ] 加载工作台 `0.16.19` / 扩展 `0.2.80` 后完成一套真实 GPT 生产验收，并核对最终 TXT 与成品库无来源旁白。
- 数据安全边界：本轮不恢复暂停队列、不重启桌面、不清理 Cookie/Session、不迁移账号目录。

## GPT-RELIABILITY-014｜运行控制真源、历史证据与首屏主操作（2026-08-10）

- [x] 服务端运行镜像使用 v3，同时保存队列和经过过滤的控制面；拒绝密码、Cookie、Token、认证凭据等敏感键。
- [x] 重启后按修订号对账，并恢复模式配置、轮换摘要、连续生产意图和各账号窗口进度；原生账号窗口自己的模式仍为最终权威。
- [x] 生产历史区分已闭环、可恢复、待核对、初始化和过程记录；旧断点不会伪装成新完成记录。
- [x] 六种模式证据仅统计当前版本带 `productionMode` 的真实闭环；没有证据时明确显示“待实测”。
- [x] 生产模式选择和主操作按钮并排进入发送区首屏；1180×760 桌面截图确认无需下滚即可继续任务。
- [x] 原 25/25 暂停任务真实恢复，完成确认、等待图片、文案、7 图下载、TXT、作品归档和素材计次迁移；队列清空且未跳过素材。
- [x] 页面声明 8 张但只有 7 个有效文件时，以作品包实物记 7 张，不伪造计划数。
- [x] 桌面四页烟测、核心 API、语法检查、针对性回归与完整自动回归通过。
- 数据安全边界：只通过现有桌面桥恢复原队列；未删除或迁移账号登录数据，未改写无关成品和素材。

## UI-RAIL-013｜退役插件市场但保留生产扩展（2026-08-09）

- [x] 主导航只保留内容生产、内容分发、流量转化、设置中心四个业务入口。
- [x] 插件市场页面、卡片数据、筛选搜索、事件监听、助手入口和专属样式全部删除。
- [x] 旧 `plugins` 活动页保存值回退到内容生产；服务端意图白名单不再产生死页面。
- [x] 内置 GPT 扩展目录、桌面加载桥、账号分区和自动生产协议保持不变。
- [x] 静态回归覆盖入口数量、死代码清理、旧值迁移和扩展内核存在性。
- [x] 普通浏览器只读跟随服务端运行镜像；只有 Electron 桌面主界面可写，避免测试页旧状态污染暂停队列。
- 数据安全边界：不启动生产，不修改账号、额度、队列、素材、成品或外部工具项目。

## UI-RAIL-012｜主导航可折叠为紧凑图标栏（2026-08-09）

- [x] 左上角三杠按钮在完整态显示“收起侧边栏”，在紧凑态显示“展开侧边栏”。
- [x] 收起后主区扩宽，所有页面入口仍以图标保留，页面切换和生产状态不受影响。
- [x] 折叠偏好写入本地存储，刷新与重启后保持；`aria-expanded` 与真实状态一致。
- [x] 180ms 克制过渡且尊重 `prefers-reduced-motion`，避免突然跳变和不必要动画。
- [x] 自动测试覆盖结构、状态持久化、68px 布局和图标显示；浏览器验收覆盖折叠、展开及刷新恢复。
- 数据安全边界：只改工作台导航布局，不启动生产、不修改账号、额度、队列、素材或成品。

## UI-LIBRARY-011｜素材与模板原地展开浏览（2026-08-09）

- [x] 素材区和模板区均提供明确的“展开 / 收起”按钮，点击卡片标题空白区域也可展开。
- [x] 展开后只显示当前资源卡片；生产控制和成品区临时隐藏但不销毁，收起后恢复原三栏布局。
- [x] 展开列表保持纵向滚动，素材和模板缩略图使用更适合预览的尺寸自动重排。
- [x] 点击、筛选、输入、键盘和滚动都会续期；10 秒无操作自动收起，`Esc` 可立即收起。
- [x] 自动测试：`public/workbench-ui.test.js` 覆盖结构、计时器、滚动续期和展开样式。
- [x] 浏览器验收：模板卡片宽度与生产网格一致（实测约 1012px），中间与右侧隐藏；10 秒后恢复。素材筛选操作后仍保持展开，新的 10 秒空闲期结束后收起。
- 数据安全边界：只修改生产页浏览交互，不启动生产、不修改账号分区、队列、素材和成品。

## GPT-PATROL-024｜巡检结果进入用户可见历史（2026-08-08）

- [x] 巡检返回原生产任务 ID 与权威成品路径。
- [x] 工作台按任务 ID 移除冲突失败/暂停行，写入唯一 completed 记录。
- [x] 没有历史任务 ID 的普通巡检使用对话 URL 生成稳定记录 ID。
- [x] Electron 0.15.18 / 扩展 0.2.70 实机重放后，本地历史同 ID 只剩一条 completed，文件数不变。

## GPT-PATROL-023｜巡检闭环记录与幂等重放（2026-08-08）

- [x] 巡检下载归档成功后发送 `tb-workbench-task-result`，包含成品路径、图片数、文案长度和素材归档路径。
- [x] 对话日志补写保存文案、下载图片和归档三类事件。
- [x] 已闭环账本记录成品元数据；再次续接只重放统计，不执行下载和归档。
- [x] Electron 0.15.17 / 扩展 0.2.69 实机重放第 7 套，返回 `already-packaged` 且成品文件数不变；用户可见历史消费由 0.15.18 完成。

## GPT-PROMPT-022｜恢复长对话中的完整文案（2026-08-08）

- [x] 助手轮次的克隆文本正常时仍清理按钮、图标和下载控件。
- [x] 清理后的克隆文本异常过短时回退到当前轮次真实可见正文。
- [x] 文案不足时抛出 `COPY_REQUIRED`，工作台停在完整性边界，不推进素材游标、不清空队列。
- [x] Electron 0.15.16 / 扩展 0.2.68 实机恢复第 7 套，验证只打包一次并写入归档记录。

## GPT-PROMPT-021｜跨重启旧队列提示词迁移（2026-08-08）

- [x] 先用真实第 7 套复现：0.15.14 恢复的旧队列仍实际发送历史短提示词。
- [x] 恢复队列时迁移精确旧默认前缀，保留素材名称及后续补充内容。
- [x] 非素材任务和用户自定义提示词不迁移。
- [x] 针对性回归 `144/144`。
- [ ] 桌面加载 0.15.15 后确认第 8 套从头发送新硬提示词全文。

## GPT-PROMPT-020｜提示词真源与最多 10 张硬约束（2026-08-08）

- [x] 工作台任务提示词会成为扩展实际发送文本，日志与网页不再使用两份内容。
- [x] 自定义环节提示词优先于任务默认提示词；两者都为空时才使用安全兜底。
- [x] 上传素材默认提示词明确完整读取附件和 TXT，并禁止 P11、分批、第二批。
- [x] 计划页数超过 10 时不回复 `1`，先要求重写。
- [x] 计划提出第二批时不回复 `1`，先要求重写。
- [x] 设置中选中的模式与当前运行模式不同时，编辑只保存到设置中的模式。
- [x] 生产运行中可查看、编辑和保存提示词，修改从下一套任务生效。
- [x] 桌面实机加载工作台 `0.15.14` / 扩展 `0.2.67`，上传环节编辑框非只读且显示硬约束全文。
- [ ] 使用新版从头上传一套真实素材，核对网页实际首条消息包含最多 10 张硬约束，并完成图片、文案、下载和归档闭环。

## GPT-PATROL-019｜单账号多对话完成文案、下载与归档闭环（2026-08-08）

- [x] 桌面实测：标题含“模板”的当前 ZWMRPG 对话自动参与，普通日常对话不参与；标题含“游戏”的候选由规则强制排除。
- [x] 桌面实测：第一批 10 张图只请求一次完整小红书文案，没有再次回复 `1`；文案首行直接为实际标题，无栏目说明和“说：”前缀。
- [x] 桌面实测：下载 10 张真实图片、保存 1 份 TXT、创建成品文件夹并归档原素材；重启后阶段稳定恢复为“作品已闭环”。
- [x] 桌面实测：从 ChatGPT 首页直接切回历史生产对话时等待标题和语义轮次恢复，不再短暂误报未命中生产标题。
- [x] 自动测试：全量 `345/345`，关键主进程、预加载和扩展脚本语法检查通过。

## GPT-PATROL-018｜模板/母版自动参与且新版生图卡不误判（2026-08-08）

- [x] 自动测试：标题含“模板”或“母版”自动参与，含“游戏”或位于手动排除名单时不参与。
- [x] 自动测试：识别 ChatGPT `data-turn` 语义轮次和“下载本组”完成入口，不把“下载 0/10”误判成未生成。
- [x] 桌面验收：当前真实对话的第一批 10 张识别为“待文案”，只发送一次完整小红书文案请求，没有重复回复 `1`。

## GPT-PATROL-017｜单账号多对话只能在双重准入后执行一个确定动作（2026-08-08）

- [x] 自动测试：标题未含“模板”或未人工准入时禁止续接；生成中、输入框有草稿、阶段不明或达到 5 次生图请求上限时禁止操作。
- [x] 自动测试：明确的 `plan-ready` 只允许回复一次 `1`，明确的 `images-ready` 只允许请求一次文案；下载归档和整批重图首版保持只读。
- [x] 安全边界：主进程只允许已在准入名单中的 ChatGPT `/c/` 链接，网页加载后再次按标题、当前 URL 和准入名单复核；每次最多一个动作。
- [ ] 桌面验收：重启至 0.15.3 / 0.2.56，仅使用 `1-zwmrpg`，依次验证一个待回复 `1` 对话和一个待文案对话；确认没有切入日常聊天、没有连续执行第二步、队列与旧模式保持暂停。

## UI-ASSISTANT-009｜小猫不得默认跨应用置顶，单击不得打开工作台（2026-08-08）

- [x] 复现条件：工作台仍打开时切到其他软件，小猫继续位于其他窗口上方；单击小猫立即打开助手/工作台。
- [x] 根因：原生小猫子窗无条件使用系统级 `floating` 置顶，主窗口失焦没有隐藏处理；小猫绑定的是单击 `chat` 动作。
- [x] 代码修复：默认 `detached=false`、`alwaysOnTop=false`；主窗与小猫窗都失焦时隐藏，重新聚焦工作台时恢复；单击改为双击；右键和完整设置新增脱离与置顶开关，未脱离时强制关闭置顶。
- [x] 自动测试：设置默认值和非法组合归一化；原生窗模式函数、失焦/聚焦钩子、双击事件、两套设置控件及复用托盘逻辑的安全重启桥接回归。新增测试先失败后修复，小猫相关 145/145、全量 340/340。
- [ ] 桌面验收：重启 0.15.2 后切到其他软件确认小猫隐藏；单击无动作、双击打开助手；分别验证“脱离但不置顶”和“脱离并置顶”。
- 数据安全边界：只改小猫窗口、交互和本地界面设置，不启动生产、不修改账号分区、队列、素材或成品。

## GPT-WEB-RECOVERY-014｜素材发送后网页静默或原生重试必须原地自愈（2026-08-08）

- [x] 真实复现：账号 2 的素材消息已包含 5 图 + TXT，但没有任何助手回复且网页不再生成；旧实现等待完整 8 分钟才续接。
- [x] 真实复现：下一套 10 图 + TXT 返回“出了点问题，请重试”，网页同时残留可用的“停止回答”按钮；点击原生重试后只返回半截计划。
- [x] 根因：通用完成等待只有“有回复稳定”和“总超时”两端，没有已提交但零回复的中间恢复点；生成中判断无法覆盖 ChatGPT 的错误页双按钮状态。
- [x] 自动测试：稳定空闲未满 60 秒不恢复，满 60 秒且零新回复/输入框为空才恢复；原生线程错误满 15 秒只重试一次，有新回复或已重试时不重复点击。
- [ ] 桌面验收：安全重启到 0.14.78 / 0.2.48 后，用同一素材分别验证静默 60 秒续接、原生重试一次、半截计划补全，并完成 `1 → 生图 → 文案 → 下载 → 归档`。

## GPT-ROTATION-RESUME-015｜全局轮换跨暂停/重启不能回落为单账号（2026-08-08）

- 现象：账号窗口均标记为“多账号全自动”，持久化轮换运行也仍在，但主选择器和继续入口显示 `single`，重启后可能由单账号调度器接管未完成队列。
- 根因：界面和调度入口只读取 `gptAutoSettings.mode`，没有把未完成的 `gptMultiRunState` 作为运行态真源。
- 修复标准：轮换状态为 `running`、`paused`、`paused-integrity-boundary`、`waiting-quota` 时，实际生产模式必须为 `rotate`；只有 `completed` 后才退回配置模式。
- 自动化测试：`public/gpt-account-rotation.test.js` 覆盖上述四种未完成状态和完成状态。
- [ ] 桌面验收：暂停轮换后重启工作台，主选择器仍显示“多账号全自动”；继续后仍只使用账号 1/2/4，并从第 15 套当前断点恢复。
- 数据边界：不得清空队列、登录分区、轮换游标或当前素材附件记录。
- 实测补充：第 15 套重启恢复后完成 10 图、文案、下载和归档；随后自动进入第 16 套。现场暴露的重复母版初始化与重复素材上传已增加独立回归保护。

## GPT-IMAGE-SILENCE-016｜确认出图后网页长耗时但没有助手首包（2026-08-08）

- 现象：系统已发送一次 `1`，ChatGPT 原生生成持续数分钟后停止，但没有新增助手消息，也没有任何图片；旧逻辑会继续等满 15 分钟。
- 修复标准：网页已停止、确认后没有新助手 turn、0 张新图且等待超过 60 秒时，在同一作品中发送图片续接指令；最多两次，不重新输出计划、不重复上传素材、不重复发送 `1`。
- 失败边界：两次仍为 0 图时返回 `IMAGE_COUNT_UNCERTAIN`，全局轮换暂停当前素材，不得标记触顶、不得跳下一套。
- 自动化测试：`gpt-automation-core.test.js` 覆盖生成中、已有新 turn、已有新图和真实无首包四种分支；全量 `306/306`。
- [ ] 桌面验收：待当前作品安全结束后加载最新扩展，再用真实无首包场景确认自动产生 `image-recovery-sent` 日志。
- 数据安全边界：始终绑定当前素材用户消息；恢复不得重传附件、不得跳过当前队列项、不得重复发送 `1`。

## GPT-QUOTA-BOUNDARY-013｜账号额度必须在作品边界完整核对（2026-08-07）
- [x] 自动测试：后来新增的账号窗口会补入账号额度设置，并继承当前默认 `80 附件 / 45 生图 / 3 小时`。
- [x] 自动测试：上传账本按全部附件计数，TXT 不再从滚动额度中漏记。
- [x] 自动测试：下一套所需附件或预计生图任一超过剩余额度时，在上传前等待；刚好可完整容纳时允许启动。
- [x] 桌面实测：重启至 0.14.76 后，account-6 的 49 张历史生图触发等待；队列保留在 14/676，当前素材未提交，ChatGPT 输入框 0 文字/0 附件；23:34:41 自动探测时间跨二次重启恢复。
- 数据安全边界：额度判断只拦截尚未启动的新作品；已进入生图的作品必须继续到文案、下载、TXT 和正式归档完成。

## GPT-GLOBAL-ROTATION-012｜全局账号轮换必须按账号自愿参与并闭环切换（2026-08-07）

- [x] 自动测试：人工控制、单账号全自动、禁用、人工暂停/停止窗口不参与；仅 `rotate` 账号参与，隐藏窗口仍可参与。
- [x] 自动测试：44/45 不提前切换；45/45 或超出安全线时，当前作品完整归档后才切换。
- [x] 自动测试：轮换跳过冷却账号；全部冷却时选择最早恢复时间并保留跨重启状态。
- [x] 桌面实测（前半环）：账号 2 连续完整归档至 `42/45`，下一套需 7 张但仅剩 3 张时在上传前等待；调度器真实切到账号 4，账号 4 完成母版初始化、上传 7 图 + 1 TXT、识别 7 页计划并自动发送一次 `1`。仍待冷却恢复后验证“账号 4 → 账号 2”的回切闭环。
- 数据安全边界：不得自动启用被禁用账号，不得打断正在生成/下载/归档的作品，不得清空跨重启队列。

## GPT-PLAN-PARTIAL-011｜半截计划不得误发 1 或重复上传（2026-08-07）

- [x] 自动测试：只有计划标题/前两行但没有可解析页数时保持等待，不判定计划完成。
- [x] 自动测试：计划解析重试沿用现有会话和附件，不重新上传当前素材。
- [ ] 桌面实测：从队列 12/676 的真实旧断点恢复，确认先补全计划，再发 `1`、生图、文案、下载和归档。
- 数据安全边界：解析失败必须保留当前队列索引，禁止上传下一套覆盖现场。

## GPT-PLAN-RESPONSE-004｜附件消息无助手回复不得跳过素材（2026-08-07）

- [x] 真实复现：两条连续任务只有 `upload-sent`，ChatGPT DOM 中存在带附件的 user turn，但其后没有 assistant turn；旧队列仍递增到下一素材。
- [x] 根因：计划等待使用全会话动态节点编号判定新回复，长会话 DOM 重排可能把旧产物算作新回复；无有效计划异常又未归类为队列完整性边界。
- [x] 修复：绑定当前附件 user turn，只读其后的助手回复；自动补发最多两次“继续处理上一条附件”恢复指令；最终失败保留当前索引并返回 `PLAN_NOT_READY` / `PLAN_NOT_COMPLETE`。
- [x] 自动测试：恢复决策纯函数、当前回复锚点、续接指令日志、连续生产重传/顺延、工作台边界分类和运行中热更新延迟均已覆盖；全量 `npm test` 283/283 通过。
- [x] 桌面实测：异常计划真实触发自动续接后，完成 `1`、10 张生图、文案、下载和正式归档；成品目录含 10 PNG、1 TXT、1 JSON，归档后才进入下一帖。
- [ ] 两条历史漏单已插入队列第 16、17 项重跑；第一条重传时触发真实上传额度边界（8/9），当前按账号额度时间自动等待恢复，待探测后继续验收两条漏单。

## GPT-AUTO-E2E-006｜单账号自动生产真实闭环（2026-08-07）

- [x] 第一账号窗口上传 6 张图片 + 1 份 TXT，日志记录 `attachmentCount=7`。
- [x] 自动识别 6 页计划并只发送一次 `1`。
- [x] 自动检测 6/6 张图片并请求小红书文案。
- [x] 自动接收 792 字文案、下载 6 张图片、保存 TXT、执行事务打包。
- [x] 正式成品目录包含 6 张 PNG、1 份非空 TXT、`GPT作品记录.json`，记录状态为 `completed`。
- [x] `D:\Download` 作为配置的图片收件箱可被授权，其他无关目录仍拒绝。
- [x] 成功归档后完成记账不再因 `uploadImages` 作用域错误误暂停。
- [x] 单账号运行中手动切换窗口时，当前帖保持原所有者，下一帖交给新选窗口。

## GPT-AUTO-CHAIN-004｜日志不得中断生产，部分附件不得继续发送（2026-08-07）

- 复现条件 1：自动上传完成后进入 `upload-sent` 对话日志节点；旧实现读取作用域外 `task`，抛 `task is not defined`。
- 复现条件 2：预计上传 N 个附件但 GPT 输入框只出现 1..N-1 个；旧改动丢失 `UPLOAD_LIMIT_SIGNAL`，统一退化为普通上传失败。
- 根因：全局日志函数隐式依赖工作流局部变量；新版发送按钮等待重构时移除了部分附件的最终分类。
- 自动测试：`GPT 对话日志不读取作用域外的 task 变量`；`附件只出现一部分时判定上传上限并停止发送`；原有 `partial GPT attachments...` 门禁。
- 桌面验收：重启后用一套有效素材验证自动选材、全量上传、计划、确认、生图、文案和下载；再用空文件夹/部分附件场景确认不会发送纯提示词或残缺素材。
- 状态：代码已修复，自动测试已通过；真实 GPT 单套闭环待验证。

## GPT-PRODUCTION-READY-001｜生产就绪检测必须识别 www.chatgpt.com（2026-08-05）

- [x] 复现条件：用户在 ChatGPT 对话界面（URL 为 `https://www.chatgpt.com/` 或 `https://www.chatgpt.com/c/xxx`）已登录并在对话中，点击"开始自动生产"无任何反应，不报错不触发。
- [x] 最早根因：`main.js` 第 1010 行 `chatConversation` 检测只认 `parsedUrl?.hostname === "chatgpt.com"`，不认 `"www.chatgpt.com"`。`productionReady` 六个条件（`contents`、`domReady`、`extensionReady`、`composerReady`、`chatConversation`、`!authenticationRequired`）中 `chatConversation` 为 false 就静默退出。同样问题存在于第 235 行 `safeGptUrl()`。
- [x] 代码修复：`main.js` 的 `safeGptUrl` 和 `chatConversation` 同时加入 `|| parsedUrl?.hostname === "www.chatgpt.com"` 判断。
- [x] 验证：`app.js` 版本号升至 0.14.45；应用已完全重启（main.js 改动必须完全重启 Electron）；GPT 扩展 0.2.28 已加载。
- [ ] 桌面实测：在 GPT 窗口处于 `www.chatgpt.com` 对话页面时点击"开始自动生产"，确认能正常触发生产流程；素材区展开分类确认明显变快。
- 数据安全边界：本修复只改 URL hostname 判断和素材加载路径，不读取、不移动、不删除业务素材或 GPT 登录态。
- 排查备忘：如果仍不触发，检查 `preflight` 对象的 `composerReady`（选择器 `#prompt-textarea, textarea[data-id="root"], [contenteditable="true"]`）和 `authenticationRequired`（页面文字含"登录/验证码"等关键词时为 true）。

## GPT-MATERIAL-LOAD-002｜素材区展开不得走完整 dashboard 重算（2026-08-05）

- [x] 复现条件：点击展开素材分类文件夹时长时间显示"正在读取文件夹…"，本地文件夹读取极慢。
- [x] 最早根因：GPT 素材区展开/勾选和 `prepareAutoGptQueue` 都走 `loadDashboard(false, categoryPath)`，每次请求 `/api/dashboard?library=xxx` 都重算分发数据、成品库、日志、生产任务索引等全部数据。N 个分类 = N 次串行完整请求。
- [x] 代码修复：`server.js` 新增 `loadAll` 选项和 `/api/materials` 轻量路由；`app.js` 新增 `loadMaterialCategory()` 函数，GPT 素材区展开/勾选改用它；`prepareAutoGptQueue` 改用 `/api/materials` 一次加载所有分类。
- [x] 验证：应用已重启；待用户确认素材展开速度明显改善。
- [ ] 桌面实测：点击展开多个素材分类，确认不再长时间卡在"正在读取文件夹…"；单账号全自动模式下不勾选素材直接点"开始"，确认自动选材逻辑正常触发。
- 数据安全边界：本修复只改素材加载路径，不读取、不移动、不删除业务素材。
- 好状态保持：`prepareAutoGptQueue` 原有的自动选素材逻辑（按使用次数从低到高、跳过隐藏文件夹、只选同时含图片和 TXT 的帖子）不变，只优化了加载路径。

## UI-CONVERSION-009｜流量转化桌面页不得再套 iframe 壳（2026-08-05）

- [x] 复现条件：在工作台打开“流量转化”时，页面主体仍是 `conversionAppFrame` iframe，TRAE/桌面元素选择只能选到 iframe 外壳，不能直接选中内部搜索框、SOP 卡片、方案匹配区域等 DOM；视觉上也像另一个独立网页嵌在软件内。
- [x] 最早根因：桌面版为了快速复用 `/conversion-integrated/`，只把独立转化应用嵌入 iframe；后续主题同步只能改善颜色，无法解决 DOM 选择边界和原生融合感。
- [x] 代码修复：`index.html` 中 `conversionView` 改为原生 `conversion-native-shell`，包含页面标题、服务状态、手机入口按钮、模块导航和 `conversionContent` 挂载点；`activateTab("conversion")` 改为调用 `loadConversionHub()`；移除旧 `ensureEmbeddedConversionApp()` / `prepareEmbeddedConversionApp()` iframe 加载路径；样式切换到透明原生 shell。
- [x] 自动测试：`workbench-ui.test.js` 新增“流量转化桌面页使用原生骨架而不是 iframe 壳”，断言不存在 `conversionAppFrame` / 流量转化 iframe，并存在原生模块导航、服务状态和内容挂载点。
- [x] 验证：新增测试先失败后修复；`node --check public/app.js public/workbench-ui.test.js public/material-workspace.test.js server.js` 通过；`node --test public/material-workspace.test.js public/workbench-ui.test.js` 111/111 通过；全量 `npm test` 251/251 通过。
- [x] 桌面实测：打开真实工作台的“流量转化”，确认能直接选中查回复输入框、模块按钮和结果区域；确认没有独立网页壳感。验证证据（2026-08-05）：重启旧 `8765` 知识库服务后，工作台 `/api/conversion/snapshot` 返回 `ok=true`，历史问答 3,520 条、方案索引 729 份；真实 Electron 窗口截图已保存为 `src/.work/conversion-search-window.png`、`src/.work/conversion-sop-window.png`、`src/.work/conversion-proposal-window.png`、`src/.work/conversion-journey-window.png`；DOM 复核显示搜索 25 张历史卡、SOP 当前环节 14 条问答、方案 12 张默认方案卡、旅程 8 个原版节点。
- 数据安全边界：本修复只改前端骨架、加载入口、样式和静态测试，不读取、不移动、不删除业务素材或客户数据。

## UI-WORKFLOW-008｜工作流编辑器与顶部界面不得挤压成摆设（2026-08-05）

- [x] 复现条件：工作流编辑器里提示词来源只有窄小图标下拉，提示词输入框不能舒适新增/修改；重试最大秒数输入框显示不完整；随机等待参数在下一行重复占空间；重试时间和等待/时间模块视觉上混在一起；顶部导航文案仍显示“素材生产（暂不开发）/内容生产（自动）/设置”；GPT 账号工具栏一排按钮像黑框；小猫拖到左侧后气泡跑到下方且箭头方向不准；小猫主体在当前界面显得过大；随机等待/失败重试残留“随机、延迟、秒后”等重复文字；设置区在电脑宽屏下仍只有两列导致大片空白；“添加环节”按钮文字占空间；顶部已有多个账号窗口时，设置里的账号管理区可能只显示旧缓存的 1 个；分发统计卡左对齐不稳；页面标题区右侧按钮错位；深色模式下 GPT 工具栏、左侧导航、服务状态、设备图标有黑框或对比不足；流量转化 iframe 不跟随深色主题；小猫图片在深浅背景下看起来有底板。
- [x] 最早根因：`renderGptModeWorkflow()` 把提示词、随机等待、检测延迟和重试配置都压在同一套窄栅格/extra 行里；顶部导航文案长期未按真实使用状态更新；小猫气泡显示后没有按真实内容高度二次定位。
- [x] 代码修复：提示词字段改为可点击放大的 `textarea`，增加“编辑提示词”按钮；随机等待秒数并入主行且去掉重复“随机”；重试参数合并成 `失败重试 X~X 秒`；数字框宽度从 48px 放宽到 68px；导航改名为“生产（暂停）/内容生产/设置中心”；GPT 工具栏扁平化；小猫气泡显示后通过 `resyncWorkbenchAssistantDockFromLauncher()` 重新贴边；小猫主体改为 `clamp()` 自适应尺寸；设置区网格改为电脑宽屏自动多列、窄屏单列；添加环节按钮改为左侧单加号；账号窗口管理区打开时刷新桌面真实账号档案并显示同步数量；分发统计卡居中；页面标题动作改为横向 flex；深色模式下给 GPT 工具栏、rail、服务状态、主题切换、设备图标补浅色半透明边界；`conversionAppFrame` 增加完整主题同步和 iframe 内主题桥；小猫图片元素强制透明背景。
- [x] 自动测试：`workbench-ui.test.js` 新增“主导航命名和 GPT 工具栏视觉保持克制”“GPT 工作流提示词和等待参数编辑器不再挤成摆设”“GPT 设置和助手控件在宽屏下紧凑自适应”“GPT 账号窗口设置跟随真实手动账号列表刷新”“分发页标题动作和统计卡保持居中紧凑”“深色模式模块使用浅色边界并同步嵌入转化页”，并增强小猫气泡定位断言。
- [x] 验证：新增测试先失败后修复；`node --check public/app.js public/workbench-ui.test.js` 通过；`node --test public/workbench-ui.test.js` 97/97 通过；全量 `npm test` 245/245 通过。
- [ ] 桌面实测：在真实工作台界面点击工作流提示词框确认会展开，随机等待不再出现重复 extra 行，重试最大秒数完整可见；拖动小猫到左侧后确认气泡在侧边且箭头正确；确认导航新文案生效。
- 数据安全边界：本修复只改前端 UI、样式和静态测试，不读取、不移动、不删除任何业务素材或 GPT 登录态。

## GPT-SCRIPT-OUTPUT-LIMIT-003｜纯脚本/沙盒输出必须按生图触顶处理（2026-08-05）

- [x] 复现条件：GPT Plus 账号撞到生图上限后，没有返回原生图片，而是输出代码解释器/脚本文件/压缩包/批量下载等沙盒产物。用户确认这也是“代码生图/撞上限”的一种表现。
- [x] 最早根因：旧口径把纯脚本/沙盒输出归为 `SCRIPT_GENERATED_OUTPUT` 普通脚本异常，只暂停但不明确纳入 `GENERATION_LIMIT_SIGNAL` 触顶信号。
- [x] 代码修复：`gpt-automation-core.js` 新增 `detectScriptOutputLimitSignal`；`sidebar.js` 的 `generatedOutputRisk` 和 `conversationStateSnapshot` 新增 `scriptOutputLimitSignal`；`script-output-limit` 现在抛出 `GENERATION_LIMIT_SIGNAL`。
- [x] 自动测试：`gpt-automation-core.test.js` 新增 5 个相关测试，覆盖无原生图+脚本文件、无原生图+压缩包/批量产物、有原生图不走纯脚本触顶、无产物不误判，以及 mock 工作流触发 `GENERATION_LIMIT_SIGNAL` 暂停决策。
- [x] 验证：`node --check` 三文件通过；`node --test gpt-automation-core.test.js` 19/19 通过；全量 `npm test` 240/240 通过。
- [ ] 桌面实测：真实 GPT Plus 窗口触顶后出现纯脚本/沙盒产物时，确认状态显示为触顶暂停，不下载、不文案、不打包、不归档。
- 数据安全边界：本修复只改变触顶分类和暂停原因，不读取、不导出、不清理 GPT 登录态，也不移动用户素材。

## GPT-PY-FALLBACK-002｜PY脚本兜底拼图与低图触顶补充特征（2026-08-05）

- [x] 复现条件：GPT Plus 账号撞到 50 张生图上限后，不用 DALL-E 原生出图，而是用 py/代码解释器拼接垃圾图敷衍；或者直接少给图，只出 4 张及以下。旧逻辑只检测重试/限额文字和纯脚本输出，有图+脚本的 PY 兜底拼图和"4张及以下"未被识别为触顶特征。
- [x] 最早根因：`generatedOutputRisk` 的 `hardFailure` 只包含 `hasRetrySignal || scriptOutput || (nativeImages===0 && artifacts>0 && hasArchiveSignal)`；当 GPT 用 py 拼接出图片时 `nativeImages>0`，虽然 `scriptOutput=true` 已能触发，但 `riskReason` 无法区分"PY脚本兜底拼图"和"纯脚本输出"；图片检测完成后 `detected.length<=4` 且 `expectedImages>4` 时只走"生成结果不足"跳过，不判定触顶。
- [x] 代码修复：①`gpt-automation-core.js` 新增 `detectPyScriptFallbackSignal`(有图+脚本特征=PY兜底拼图)和 `detectLowImageLimit`(≤阈值)；②`sidebar.js` 的 `generatedOutputRisk` 新增 `pyScriptFallbackSignal`、`lowImageLimit`、`lowImageCount` 字段，`hardFailure` 加入 `pyScriptFallback.detected`；③`riskReason` 新增 `"py-script-fallback"` 分类；④`waitForGeneratedImageGrowth` 中 PY 脚本兜底拼图错误消息明确指出"PY代码兜底拼接垃圾图"；⑤图片检测完成后 `detected.length<=4 && expectedImages>4` 判定为 `GENERATION_LIMIT_SIGNAL`；⑥`conversationStateSnapshot` 返回 `pyScriptFallbackSignal` 和 `lowImageLimit`；⑦`stateSnapshot` 检查加入 `pyScriptFallbackSignal`。
- [x] 自动测试：`gpt-automation-core.test.js` 新增 10 个测试用例（5个PY脚本兜底拼图 + 5个低图触顶），覆盖有图+脚本、有图+脚本文件、无图不判、无脚本不判、4张检测、5张不检测、0张不检测、默认阈值等边界。
- [x] 验证：`node --check` 三文件通过；`node --test gpt-automation-core.test.js` 14/14 通过；全量 `npm test` 235/235 通过。
- [ ] 桌面实测：在真实 GPT Plus 窗口中撞到生图上限后，确认 py 拼接垃圾图被识别为 `GENERATION_LIMIT_SIGNAL` 并暂停当前账号窗口；确认只出4张且计划要求更多时也判定为触顶。
- 数据安全边界：本修复只改变触顶检测逻辑，不读取、不导出、不清理 GPT 登录态，也不移动用户素材。

## GPT-WAIT-KEYWORD-001｜等待环节关键词完成信号必须生效（2026-08-05）

- [x] 复现条件：工作流要求 GPT 在计划、出图、文案结束时分别回复固定关键词，例如“计划完成”“出图完毕”“文案完成”；旧逻辑只有 `detect-plan` UI 暴露了 `pattern`，但执行层未读取，`wait-images` 和 `wait-copy` 没有关键词入口。
- [x] 最早根因：`sidebar.js` 中 `detect-plan` 硬编码 `/迁移计划|逐页|P\s*1/i`；`wait-plan` 只按计划结构判断，`wait-images` 只按图片数量/完成动作判断，`wait-copy` 只按文案长度和文案特征判断，三者没有统一读取 UI 参数中的完成关键词。
- [x] 代码修复：`wait-plan`、`wait-images`、`wait-copy` 新增 `keywordPattern` 参数并在执行层读取；关键词命中作为完成证据之一，原有签名稳定、生成停止、图片数量、文案质量检测保留。
- [x] 自动测试：新增 `integrations/gpt-production-extension/gpt-automation-core.test.js`，用模拟回复验证计划、图片、文案关键词正则可命中，空配置回退默认值，无效正则不抛异常。
- [x] 验证：`node --check integrations/gpt-production-extension/gpt-automation-core.js; node --check integrations/gpt-production-extension/sidebar.js; node --check public/app.js` 通过；`node --test integrations/gpt-production-extension/gpt-automation-core.test.js` 4/4 通过；全量 `npm test` 225/225 通过。
- [ ] 桌面实测：在内容生产工作流编辑器中把三个等待环节关键词改为自定义值，让 GPT 回复这些关键词，确认对应等待能结束并进入下一环节；文案仍需通过质量检查后才保存/打包。
- 数据安全边界：本修复只改变网页等待检测，不读取、不导出、不清理 GPT 登录态，也不移动用户素材。

## DATA-DISTRIBUTION-007｜空作品集不得进入可发库存（2026-08-05）

- [x] 复现条件：在真实阶段文件夹 `抖音小红书` 下存在带 `[泛]` / `[转]` 标签的作品集目录，但该目录为空、只有空子目录，或子目录只有 TXT/Markdown 没有图片。
- [x] 最早根因：`src/lib/distribution-data.js` 的 `inspectSource()` 只用子目录数量判断作品集有效，`itemCount > 0` 即 `valid=true`，没有检查子作品目录是否包含真实图片。
- [x] 代码修复：`inspectSource()` 改为全量检查子目录图片数量，只把 `imageCount > 0` 的子作品计入 `itemCount`；界面预览仍截取前 50 个，但有效性不再依赖预览截断。
- [x] 自动测试：新增 `lib/distribution-scan-validity.test.js`，覆盖空目录、空子目录、只有文案、真实含图目录，以及有效子目录排在 50 个预览项之后的边界。
- [x] 验证：`node --test lib/distribution-scan-validity.test.js` 2/2 通过；全量 `npm test` 221/221 通过。
- [x] 桌面实测：打开内容分发 → 抖音小红书可发，确认实际业务目录中的空作品集不再显示为可发库存；真实含图片作品集仍可展开预览和发送。验证证据（2026-08-05）：API 显示 19 个 sourceValid=true 的作品集全部有 itemCount>0；50 个空作品集全部 sourceValid=false；之前有问题的 作品集_020[泛] 已正确标记为不可发；浏览器界面"抖音小红书可发"标签显示 12 个有效作品集，空作品集不再出现。
- 数据安全边界：本修复只读取目录并改变库存判定，不移动、不删除、不重命名任何用户作品文件。
- 状态：已通过桌面人工复验（2026-08-05），标记为永久回归。

## 0.14.27｜automatic 模式边界测试与版本号同步（2026-08-03）

- [x] automatic 模式专属边界测试：新增 2 个自动测试用例，锁死 automatic（自动模式）三个硬边界——不连续（不永不停歇）、不补充素材、不调度下一轮；触顶恢复只续剩余队列。
- [x] 版本号同步修复：`index.html` 中 `styles.css?v=0.14.25` 未与 VERSION(0.14.26) 同步，导致"开发版静态资源缓存版本与 VERSION 同步"测试失败；已修复为 0.14.26。
- [x] 验证：`workbench-ui.test.js` 82/82 通过（新增 2 个 automatic 测试）；全量 `npm test` 199/200，1 个失败为 `work-package duplicate output` 环境依赖问题（本地打包程序未配置），与本轮改动无关。
- [ ] 桌面实测：选择 1 个素材帖子，切到"自动模式"，点"开始自动生产"，确认自动完成 上传→等计划→发1→等图→求文案→打包归档 全链路。
- [ ] 桌面实测：选择 2-3 个素材帖子，启动自动模式，确认严格逐帖串行（前一帖打包归档后才启动下一帖），队列跑完即停，不自动补充新素材。
- [ ] 桌面实测：自动模式运行中点"暂停"，确认停在安全检查点；点"继续"从当前帖子恢复。
- [ ] 桌面实测：自动模式运行中点"停止"，确认终止本轮不删队列和登录状态；点"启动"可重新开始。
- [ ] 桌面实测：自动模式触顶（低图数 1-3 张）后暂停并安排额度探测；探测恢复后只续剩余队列，不补充新素材。
- [ ] 桌面实测：自动模式完成后，确认成品目录包含图片、非空 TXT、GPT作品记录.json，素材归档到数字目录。

## 0.14.28 模式名称统一、停止 bug 修复与 6 模式补齐（2026-08-04）

- [x] 修复停止按钮 bug：停止后 gptAutoRunning 未清零，导致模式切换被 guard 拦截。已在停止分支加入 gptAutoRunning = false。
- [x] 停止按钮 window.confirm 改为 openSystemDialog，避免被 GPT 视图层遮挡。
- [x] 模式名称统一：手动模式→人工控制、自动模式→选材后自动、单账号·永不停歇→单账号全自动、多账号轮换·永不停歇→多账号全自动。
- [x] 新增定时单账号全自动模式（scheduled）：单账号全自动+定时窗口。
- [x] 新增单账号多对话巡检模式（patrol）：实验模式，默认关闭。
- [x] GPT_MODE_DEFINITIONS 更新为 6 种模式 + semi-auto 兼容保留。
- [x] index.html 下拉框更新为 6 种模式选项。
- [x] normalizeGptProductionMode 新增 scheduled/patrol 别名映射。
- [x] isContinuousGptMode 新增 scheduled/patrol 为连续模式。
- [x] node --check public/app.js 通过。
- [x] npm test 通过（除已知环境问题）。
- [ ] 桌面实测：切换到 6 种模式，确认下拉框、提示行、说明面板正确显示。
- [ ] 桌面实测：启动单账号全自动→点停止→确认可以切换到其他模式（bug 修复验证）。
- [ ] 桌面实测：scheduled 模式的工作时段限制与恢复。
- [ ] 桌面实测：patrol 模式标记为实验，默认不自动启动。

## 6 模式回归清单（V1.0）

### A. 人工控制（manual）
- [ ] 选择一个帖子后只上传该帖子附件
- [ ] 上传按钮不自动点击发送
- [ ] 可以人工填写提示词
- [ ] 可以人工发送 "1"
- [ ] 手动下载前必须已有文案
- [ ] 手动打包不会产生缺 TXT 半成品

### B. 选材后自动（automatic）
- [ ] 单个帖子完整生产
- [ ] 多个帖子严格串行
- [ ] 父合集正确拆帖
- [ ] 当前帖子未结束前不上传下一帖
- [ ] 计划后只发送一次 "1"
- [ ] 图片完成后自动请求文案
- [ ] 文案完成后自动保存
- [ ] 图片与 TXT 统一打包
- [ ] 成功后才增加使用次数
- [ ] 失败素材留在原位置
- [ ] 队列跑完即停，不自动补充新素材

### C. 单账号全自动（single）
- [ ] 优先处理用户已勾选的帖子
- [ ] 勾选队列处理完后从全素材库选材
- [ ] 按使用次数从低到高选择
- [ ] 触顶后暂停并记录下次探测时间
- [ ] 到探测时间后自动恢复
- [ ] 恢复后只续剩余队列，不补充新素材
- [ ] 跨重启从检查点续跑
- [ ] 工作时段外不领取新任务

### D. 定时单账号全自动（scheduled）
- [ ] 工作时段内（07:00-02:00）自动生产
- [ ] 非工作时段不领取新任务
- [ ] 当前帖子允许安全完成
- [ ] 软件启动后自动检查是否可继续
- [ ] 联网恢复后自动继续
- [ ] 电脑唤醒后自动继续

### E. 多账号全自动（rotate）
- [ ] 每个账号一次只领取一个帖子
- [ ] 账号A触顶只暂停账号A
- [ ] 账号B、C继续运行
- [ ] 禁止把账号A素材注入账号B
- [ ] 切换可见窗口不影响任务绑定
- [ ] 所有账号共享素材占用锁

### F. 单账号多对话巡检（patrol）【实验】
- [ ] 识别对话有出图计划→发1
- [ ] 识别对话有完整图片→求文案
- [ ] 识别对话有完整文案→下载归档+上传新素材
- [ ] 识别对话只有1-3张图→限额处理
- [ ] 识别对话空白→标记不可识别
- [ ] 巡检完成后循环

## 0.14.22｜设置面板合并、实时保存与拖拽排序

- [x] 删除顶部重复字段：`gptModeConfirmText`（自动确认文字）和 `gptModeCopyPrompt`（文案请求词）已移除；这些值现在直接从工作流步骤 `send-confirm` 和 `request-copy` 的 text 字段读取。
- [x] 删除底部重复开关：`gptAutoConfirmEnabled`、`gptAutoCopyEnabled`、`gptAutoPackageEnabled` 已移除；启用状态现在直接从工作流步骤的 enabled 复选框派生。
- [x] 删除隐藏的 legacy 字段：`gptConfirmText` 和 `gptCopyPrompt`（限速区内 hidden 标签）已移除。
- [x] 删除"保存当前模式"按钮：`gptSaveModeProfileBtn` 已移除；所有修改实时保存，无需手动点击保存。
- [x] 合并设置面板：原"模式与归档"+"自动生产闭环"两个 section 合并为统一的"生产工作流" section；母版迁移器规则和自定义额外规则移入该 section。
- [x] `saveGptAutoSettings` 重构：不再用独立字段值覆盖工作流步骤；改为从工作流步骤派生 `confirmText`/`copyPrompt`/`autoConfirm`/`autoCopy`/`autoPackage`。
- [x] 实时保存：工作流编辑器添加 `input`（静默保存）和 `change`（保存+toast+验证）事件监听；模式名称和第一环节也加入 `change` 实时保存。
- [x] 拖拽排序：工作流步骤行添加 `draggable="true"` 和拖拽手柄（⠿）；`dragstart`/`dragover`/`drop`/`dragend` 事件实现 HTML5 拖拽排序；输入框内拖拽不触发排序。
- [x] CSS 视觉反馈：`.dragging`（半透明缩小）、`.drag-over`（高亮边框+阴影）、`.gpt-workflow-drag-handle`（hover 渐显）。
- [x] `node --check src/public/app.js` 通过、全量测试 `239/239` 通过。
- [x] 桌面实测：设置面板只显示一个"生产工作流" section，无重复字段。
- [x] 桌面实测：修改工作流步骤文字（如改"1"为"确认"）后关闭设置面板，再次打开确认修改仍在。
- [x] 桌面实测（UI层）：取消勾选某个步骤的"启用"后，checkbox 状态持久化到 localStorage（`teambuilding-gpt-mode-profiles-v1`）。实际生产流程跳过需 GPT 环境验证。
- [x] 桌面实测：拖拽步骤行可排序，拖拽时目标行高亮，松手后顺序立即保存。
- [x] 桌面实测：输入框内可正常选择文字，不会触发拖拽。
- [x] 桌面实测：母版规则和额外规则在"生产工作流" section 内可见可编辑。

## 0.14.21｜半自动模式、母版规则可视化与连续模式状态增强

- [x] 半自动模式定义：`GPT_MODE_DEFINITIONS` 新增 `semi-auto` 项，`normalizeGptProductionMode` 支持映射，`gptModeProfiles` 新增默认配置。
- [x] 半自动任务暂停：`plannedOnly` 结果时设置 `gptSemiAutoPendingTask`，暂停队列，显示"确认继续出图"按钮。
- [x] 半自动恢复：`resumeSemiAutoGptTask` 设置 `_semiAutoResume` 标记后恢复队列，不再二次暂停。
- [x] 发送按钮 guard：`gptSemiAutoPendingTask` 存在时按钮禁用并显示"等待确认计划"；点击事件加 guard 防竞态。
- [x] 模式切换清 pending：`handleGptModeChange` 在切换到非 semi-auto 模式时清除 `gptSemiAutoPendingTask`。
- [x] 暂停按钮不绕过确认：`gptPauseQueueBtn` 点击时检测 pending 并提示"请点确认继续出图"。
- [x] 跳过按钮清 pending：`gptSkipTaskBtn` 点击时清除 pending 并恢复队列状态。
- [x] 停止按钮清 pending：`gptStopQueueBtn` 停止时清除 pending，"确认继续出图"按钮消失。
- [x] 账号切换清 pending：`switchGptAccount` 在 `accountChanged` 时清除 `gptSemiAutoPendingTask`。
- [x] catch 路径不误报失败：`isSemiAutoPause` 判断跳过 `gptLastFailedTask` 设置和告警弹窗。
- [x] V3.6 母版规则可视化：设置面板新增只读 `gptMasterPromptRules` 显示母版规则全文。
- [x] 自定义额外规则：`gptExtraPromptRules` 字段可编辑，保存到 `gptAutoSettings.extraPromptRules`，注入时追加到母版规则之后。
- [x] 连续模式状态增强：`updateGptAssistantBubble` 显示等待额度恢复/工作时段外/已就绪/运行中/未启动。
- [x] `node --check src/public/app.js` 通过、全量测试 `183/183` 通过。
- [x] 桌面实测：设置面板可看到 V3.6 母版规则全文，额外规则可编辑并保存。
- [ ] 桌面实测：半自动模式上传素材后，GPT 生成计划时暂停并显示"确认继续出图"按钮。（需GPT实际交互验证）
- [ ] 桌面实测：点击"确认继续出图"后自动完成出图、文案和打包。（需GPT实际交互验证）
- [ ] 桌面实测：等待确认期间发送按钮禁用，暂停/跳过/停止按钮均正确清除 pending。（需GPT实际交互验证）
- [ ] 桌面实测：连续模式状态栏显示"等待额度恢复（约X分钟后探测）"或"工作时段外"。（需Electron环境验证）

## 0.14.20｜模式名称简化、GPT 视图层级修复与模式说明

- [x] 模式名称简化：`单账号单模板，等待循环+手动窗口` → `单账号·永不停歇`；`单账号单模板，等待循环+自动窗口` → `多账号轮换·永不停歇`。下拉框不再截断。
- [x] 模式选择器下方新增动态提示行（`gptModeHint`），实时显示当前模式的用途说明。
- [x] 设置面板模式选择器旁新增 `?` 按钮（`gptModeInfoBtn`），点击展开/收起模式详细说明（`gptModeInfoPopover`）。
- [x] `GPT_MODE_DEFINITIONS` 新增 `shortName` 和 `description` 字段，供账号标签和说明面板使用。
- [x] **GPT 视图层级修复**：`showEmbeddedGptView` 新增 6 项前置守卫——非 GPT 标签页、设置面板打开、系统对话框打开、图片大图打开、助手面板打开、历史面板打开、右键菜单打开时均不显示原生 GPT 视图。
- [x] `restoreEmbeddedGptView` 的 3 个延迟回调（rAF / 180ms / 700ms）均增加当前标签页检查，防止切换标签后延迟回调把 GPT 视图显示到非 GPT 页面上。
- [x] `openPageSettings` 改为 `async` 并 `await` GPT 视图隐藏，消除设置面板打开瞬间的 GPT 残影。
- [x] 多账号模式状态消息同步更新为新名称（`多账号轮换·永不停歇`）。
- [x] `node --check` 通过、全量测试 `239/239` 通过。
- [x] 桌面实测：切换到分发/转化/工作流等非 GPT 标签后，GPT 原生视图不再浮在页面之上。
- [ ] 桌面实测：打开设置面板时 GPT 视图立即隐藏，无残影闪烁。（需Electron环境验证，浏览器中WebContentsView API不存在）
- [x] 桌面实测：模式选择器下拉框完整显示所有选项名称，下方提示行正确显示当前模式说明。（CSS修复select width:100%）
- [x] 桌面实测：设置面板 `?` 按钮点击后展开说明，再次点击收起。

## 0.14.19｜每窗口独立模式与 P0 修复

- [x] 每个账号窗口记住自己的生产模式（手动/自动/永不停歇/轮换）；切换窗口时各自恢复，不串用。
- [x] 账号标签显示当前模式简称；悬停可看完整模式名。
- [x] 暂停生产时不再锁定素材和模板选择（`blockGptSelectionDuringRun` 在 `gptAutoPaused` 时返回 false）。
- [x] 右键菜单在 GPT 视图活动时先隐藏 WebContentsView，关闭后延迟恢复（`contextMenuGptHidden` 标志管理）。
- [x] 分发快照确认为废弃功能，不再纳入回归测试。
- [x] DPAPI 安全存储修复：`powershell.exe` 改为绝对路径，解决 Node 测试环境和 Electron 中 PATH 不包含 PowerShell 的问题。
- [x] 手动模式防重复上传：`gptCurrentManualTask` 等待期间发送按钮禁用并显示"等待手动发送"；点击事件加 guard 防竞态。
- [x] 账号切换清状态：`switchGptAccount` 在 `accountChanged` 时清除 `gptCurrentManualTask`，防止旧窗口手动任务串到新窗口。
- [x] `node --check src/public/app.js`、全量测试 `183/183` 通过（含 DPAPI 修复后 0 失败）。
- [ ] 桌面实测：窗口1设手动、窗口2设自动，切换标签后各自模式保持不变。
- [ ] 桌面实测：暂停后可直接选择新素材，不需先停止。
- [ ] 桌面实测：GPT 页面活动时右键菜单可见且可操作。
- [ ] 桌面实测：手动模式上传后发送按钮禁用，"完成当前，上传下一套"可见。

## 0.14.18｜窗口恢复与自动重试

- [x] 切换账号窗口先恢复该窗口 quota snapshot；共享额度组按 `quotaGroup` 查询。
- [x] 连续模式临时网页/桥接错误自动重试当前阶段，不提前注入下一帖。
- [x] 暂停、停止会取消对应窗口 retry timer；继续/启动保留队列、设置和登录状态。
- [x] `node --check src/public/app.js`、工作台 `194/194`、GPT 扩展 `41/41`。
- [ ] 两个真实 Plus 账号桌面验证：切换窗口后各自 quota、上传确认数和自动重试与网页状态一致。

## 0.14.17｜账号窗口恢复、暂停/停止与自动探测

- [x] 每个账号窗口独立保存运行状态、当前阶段、上传确认数和生图数；切换窗口不重置另一窗口的检查点。
- [x] 单账号单模板模式只在当前选中的账号窗口继续，不会在切换标签时把素材注入其他窗口。
- [x] 暂停/继续与停止/启动语义分开：暂停保留当前阶段，停止禁止自动上传和自动重试但保留队列与登录分区。
- [x] 首次额度探测按本轮第一次真实消耗起点计算；探测失败后随机 10–20 分钟自动重试。
- [x] 附件上传确认后立即更新小猫助手的 `x/y` 数量，轮询阶段更新本轮生图数量。
- [x] 轮换模式切换账号前激活对应持久浏览器档案，并保持一帖一窗口顺序。
- [x] 自动回归：`node --check src/public/app.js`、工作台 `75/75` 通过。
- [ ] 真实桌面复验：在两个已登录账号窗口中确认窗口 1 触顶后窗口 2 接管；本轮未消耗真实额度执行该项。

## DATA-MATERIAL-006｜使用次数目录统一为阿拉伯数字

- [x] 新归档目录由 `materialUsageDirectoryName()` 统一生成 `1`、`2`、`3`、`4` 等纯数字目录。
- [x] 旧的“已使用/已上传/已制作 N 次”目录仍可推断次数；不会因兼容读取而继续新建旧命名。
- [x] 数字目录只读取素材库根目录的第一层，帖子标题中的日期或数字不会被误认为使用次数。
- [x] 归档时以账本次数与物理目录次数的较大值为基线再加一，避免重复归档错位。
- [x] 自动测试覆盖数字目录、旧目录兼容和普通数字标题不误判。

## 0.14.14 成品路径与原素材关联门禁

- [x] 最终成品目录固定为工作台配置中的 `D:\AICode\项目推进\projects\江湖有旅人\主项目\成品库（GPT+本地脚本制作）`；`D:\Download` 只作为下载暂存目录。
- [x] `GPT作品记录.json` 写入 `sourceMaterialPath`、`sourceMaterialName`；素材归档后另写 `sourceMaterialArchivePath`，原始路径不被覆盖。
- [x] 生产历史检查点返回原素材路径，用户可从“查看生产记录”反查来源。
- [x] 通过追加日志中的精确 `packagePath`/`from` 证据补全历史记录；无法精确匹配的旧记录保持原样，不按标题猜测。
- [x] 开发版重启后 `/api/gpt-production/history` 能返回已补全的来源路径；工作台 `192/192`、扩展 `41/41` 通过。

这不是待办便签，而是每次开发和交付都必须执行的质量门禁。用户已经报告过的问题一旦进入本库，后续版本不得复发。

## 0.14.13 GPT 临时缓存与触顶信号门禁

- [x] 每套生产任务完成并真实落盘后刷新当前 GPT 账号窗口；不会在上传、出图、文案、下载或打包中途刷新。
- [x] 默认三小时维护只调用 `session.clearCache()`/忽略缓存刷新；代码与测试确认维护路径不调用 `clearStorageData()`。
- [x] 维护前后 Cookie、localStorage、Google/ChatGPT 登录分区、扩展状态和生产记录保持原值。
- [x] “出了点问题，请重试 / Something went wrong”在出图上下文被记录为候选触顶信号；网络、离线、超时、验证码和登录错误不会直接误判为触顶。
- [x] 触顶/低产出后当前帖子不继续盲目重试、不把残图打包，队列保存阶段和下一次探测时间。
- [x] 工作台 `192/192`、生产扩展 `41/41`、主进程/预加载/扩展语法检查通过。
- [ ] 真实 Plus 窗口触发一次“请重试”后，仍需在用户允许消耗额度的实际生产窗口中复验网页原始提示与本地判定一致。

## 使用规则

## UI-ASSISTANT-005 小猫对话框高度

- [x] 点击小猫后对话框整体高度收紧到原来的约一半，不覆盖大块 GPT 工作区。
- [x] 消息区保留滚动，底部输入和执行按钮仍可操作；关闭对话后恢复原生 GPT 页面。
- [x] 静态回归断言确认消息区上限为 `min(150px, 22vh)`、对话框上限为 `min(300px, 46vh)`。

1. 开发前先查本次改动会影响哪些编号。
2. 修复必须写清：复现条件、最早根因、代码修复、自动测试、桌面实测。
3. 没有自动测试的项目必须说明原因，并保留可重复的人工步骤。
4. “当时看起来正常”不算通过；必须经过触发原问题的完整操作序列。
5. 登录态、任务队列、用户文件等高风险状态，验证前先确认不会被测试破坏。
6. 相关回归项没有全部打勾，不允许更新正式使用入口或宣布完成。

## 0.14.12 本轮轮换模式门禁

- [x] 模式选择器包含手动、自动、永不停歇、单窗口多浏览器轮换四个用户可见模式；旧多账号值仅保留兼容，不作为默认入口。
- [x] 轮换模式一次只处理一帖；同一账号完成成功后不自动切换，只有真实限额、低图数/降级信号才切换账号窗口。
- [x] 当前帖子在切换账号时保留 requestId、阶段和附件状态；不会把下一帖提前注入正在等待计划/出图/文案的窗口。
- [x] 所有账号窗口触顶时安全暂停并持久化下一次探测时间；不清理登录分区、Chromium 缓存、用户设置或生产记录。
- [x] 静态回归与 Node 自动测试：工作台 `191/191`，扩展 `41/41`，`node --check` 通过。
- [ ] 桌面真实双账号轮换：需在用户允许消耗额度的窗口中验证“窗口 1 真实触顶后窗口 2 接管”，本轮未宣称通过。

## 交付门禁

- [ ] 相关自动测试全部通过。
- [ ] 真机按原始复现步骤再次操作。
- [ ] 检查修复没有清缓存、删除登录态或重置用户设置。
- [ ] 检查运行中的生产任务没有因界面切换而中断。
- [ ] 记录最终版本、提交、测试数量和仍存在的限制。

## 活跃回归清单

### MAINT-001｜独立扩展与工作台内嵌副本版本漂移

- 现象：独立扩展代码已更新，但工作台内嵌 `manifest.json` 仍停留在旧版本，导致开发测试通过而实际加载旧执行器。
- 根因：同步时只复制了脚本文件，没有把清单版本纳入一致性门禁。
- 修复标准：独立扩展与内嵌副本的 `manifest.json`、核心脚本和 vendor 脚本必须逐项哈希一致；每次扩展版本更新同步提升内嵌清单。
- 自动测试：扩展清单版本断言；发布前执行源文件/内嵌文件哈希对比。
- 状态：代码已修复，待本轮完整测试。

### FLOW-GPT-009｜会话状态不明确时提前注入下一帖

- 现象：当前对话还处于等待计划、等待图片或等待文案阶段，工作台又上传了下一帖；脚本/Python 降级输出还可能被误当成品。
- 根因：旧桥接主要依赖局部按钮和计时；GPT 正文、图片和脚本附件又可能分散在同一 `conversation-turn` 外层的兄弟节点，导致计划/出图状态取证不完整。
- 修复标准：网页执行器必须返回当前会话标签、最新助手正文、图片数、计划/确认/文案/限额/脚本信号和 `canInjectNext`；计划还必须有页数和“回复 1/暂不出图/原生复制动作”等完成证据；只有完成、明确失败跳过或安全空闲才允许领取下一帖。
- 自动测试：扩展状态快照、脚本降级停止、当前会话无提示词复用、回复外层取证和 `PLAN_NOT_COMPLETE` 暂停测试。
- 低产量触顶判定：按后端文件 ID 去重后再判断 1–3 张；迁移计划必须保持 8 秒稳定后才允许发送 `1`，防止重复上传下一帖。
- 桌面验收：真实计划完成后 1–5 秒只发一次 `1`；等待图片期间不得出现下一帖附件；脚本/Python 输出立即暂停。
- 状态：代码与自动测试已补，待真实桌面复验；队列当前保持暂停。

### FLOW-GPT-011｜单账号上传上限只进入部分附件

- 现象：一次选择 7–8 个图片/TXT 附件，GPT 输入框只出现其中 1–3 个；旧逻辑可能等待后按普通附件失败处理，无法告诉用户是上传额度还是网络问题。
- 根因：上传入口只返回“附件尚未全部就绪”，没有把实际进入输入框的数量作为触顶证据传回工作台。
- 修复标准：扩展在 `实际附件数 < 预期附件数` 且等待超时后返回 `UPLOAD_LIMIT_SIGNAL`，错误中包含 `实际/预期`；工作台按上传限额记录本轮起点、下一次探测时间并暂停当前账号，不继续注入下一帖。
- 自动测试：扩展架构测试断言 `UPLOAD_LIMIT_SIGNAL`、部分附件文案；工作台回归测试断言上传信号进入真实额度判定。
- 桌面验收：故意在上传窗口触顶时选择多附件，确认小猫和状态栏显示“上传未完整 x/y、预计恢复时间”，当前输入框不再叠加下一帖。
- 状态：代码与静态测试已完成，待真实 GPT 上传上限场景复验。

### UI-ASSISTANT-004｜点击小猫后对话框被 GPT 原生视图覆盖

- 现象：点击小猫后，工作台对话框看似打开但被右侧 GPT 原生页面压住，或原生助手浮层重新盖回对话框。
- 根因：原生 `WebContentsView` 是独立合成层；旧逻辑先显示 DOM 对话框再异步隐藏原生层，且后台状态更新可能重新显示原生小猫。
- 修复标准：打开对话先 `await gptWorkbench.hide()`，再显示 DOM 对话框；对话期间 `assistantChatOpen` 锁定原生浮层为隐藏；关闭后恢复原生 GPT。小猫、状态气泡、对话框使用统一最高层级变量。
- 自动测试：工作台断言隐藏先于打开、`assistantChatOpen` 锁定和对话框最高层级 CSS。
- 桌面验收：在 GPT 页面、深色主题和窗口最小化恢复后点击小猫；对话框固定在右侧、输入框可点击，关闭后 GPT 页面仍在原会话。
- 状态：代码与静态测试已完成，待桌面真实点击复验。

### FLOW-GPT-010｜生成图缩略图被漏计或签名 URL 被重复计数

- 现象：同一组回复有一张大预览和多张 48×48 缩略图；旧逻辑过滤小图后把 6 张算成 1 张。ChatGPT 同一文件的缩略图与全尺寸 URL 签名不同，又可能把一张算成多张。
- 根因：通用 `imageUrl` 尺寸过滤直接作用于生成图；去重按完整签名 URL 而不是稳定后端文件 ID。
- 修复标准：当前回复外层的生成图专用选择器允许缩略图计数；普通兜底图片仍过滤小图；`backend-api/estuary/content?id=file_*` 按 `id` 归一化去重；脚本/沙盒附件不能替换原生图片列表。
- 自动测试：扩展稳定文件 ID 去重、回复外层取证和小图允许计数断言；工作台 `190/190`、扩展 `41/41`。
- 桌面验收：真实回复滚动到可见后，检查 48px 缩略图全部纳入当前组；同一文件的不同签名只计一次；图片数量低于安全线时才暂停。
- 状态：代码和自动测试已完成；已用真实 GPT DOM 观察到正文节点 `0` 图、外层 `19` 图/`6` 个稳定文件 ID，补丁已覆盖该结构。

### UI-GPT-001｜最小化或退到后台后 GPT 白屏

- 状态：已修复，永久回归。
- 原始现象：GPT 页面和任务实际仍存在，但最小化、托盘隐藏后恢复工作台时，右侧只显示白色占位层。
- 复现条件：进入“内容生产（GPT）”并显示原生 GPT 视图 → 最小化或关闭到托盘 → 恢复工作台。
- 根因：最小化时主动隐藏 `WebContentsView`；Windows 恢复最小化窗口触发 `restore`，旧代码只监听 `show`，因此没有重新显示原生视图。
- 修复标准：同时监听 `show` 与 `restore`；恢复后重新挂载原生绘制层但不 reload ChatGPT；保持原会话 URL、登录态和生产阶段；容器重新具有 `is-native-visible`。
- 自动测试：`public/workbench-ui.test.js` 中“restoring a minimized workbench reattaches the live GPT surface without reloading it”。
- 桌面验收：真实最小化后恢复；确认 GPT 可见、扩展 ready、任务仍 running 且阶段未倒退。
- 最近证据：2026-08-01 实机通过；生产任务保持“等待图片”；提交 `caa6610`。

### UI-GPT-002｜GPT 视图漂浮、越界或滚动错层

- 状态：已修复，永久回归。
- 复现条件：内容生产页滚动左侧素材树、切换主题、调整窗口尺寸。
- 修复标准：GPT 使用宿主精确边界；仅一层 16px 圆角；左侧独立滚动不移动 GPT；不得出现 12px 重复内缩。
- 自动测试：`public/workbench-ui.test.js` 中嵌入 GPT 与主题相关断言。
- 桌面验收：左右区域分别滚动、缩放窗口、切换四套主题后检查边界。

### UI-GPT-003｜GPT 页面工具重复或错绑用户上传内容

- 状态：已修复，永久回归。
- 修复标准：只在助手生成回复上显示一组“下载本组／下载并打包／复制文案／下载 TXT”；用户上传素材不显示；按钮绑定当前回复批次。
- 自动测试：扩展测试中的当前批次图片绑定、文案提取与下载打包用例。
- 桌面验收：长对话同时存在旧回复、用户上传图和新生成图时逐项核对。

### FLOW-GPT-001｜自动生产停在计划后不发送 1

- 状态：已修复，永久回归。
- 修复标准：稳定识别计划正文和发送按钮状态；每套只发送一次 `1`；恢复任务不得重复发送。
- 自动测试：扩展的计划识别和单次确认测试。
- 桌面验收：真实 Plus 会话从上传附件运行至图片阶段。

### FLOW-GPT-002｜任务运行时重新勾选导致当前队列变化

- 状态：已修复，永久回归。
- 修复标准：生产开始后冻结素材、模板、浏览器和作品数量；新选择只进入下一批；暂停、继续和跳过不得重建当前批次。
- 自动测试：`public/workbench-ui.test.js` 中队列锁定和暂停继续用例。

### FLOW-GPT-003｜图片或文案不完整却误报打包成功

- 状态：已修复，永久回归。
- 修复标准：计划页数等于本轮图片数；文案满足最低长度；TXT、图片和任务清单批次一致；**本轮文案/TXT 是最终下载和打包的硬前置**；失败跳过当前素材，不伪造完成、不归档原素材。
- 自动测试：当前批次下载、最小图片数、文案前置、任务清单和查重测试。
- 桌面验收：分别模拟 2–3 张图片、缺 TXT 和重复图片。

### FLOW-GPT-004｜文案未完成却提前下载或生成半成品

- 状态：已修复，永久回归。
- 复现条件：图片已生成但文案请求尚未完成，自动流程继续下载并调用打包；或手动点击“下载并打包”时没有本轮文案/TXT。
- 根因：旧自动流程先启动图片下载，再等待文案；手动按钮只校验图片，未把文案作为同一批次的硬前置。
- 修复标准：先等待并校验本轮文案（自动流程可见字符不少于 300）；通过后先写入下载目录 `.gpt-copy-staging/<batchId>.txt`，再允许最终图片下载、写入成品 TXT、查重打包和归档。手动按钮也先读取剪贴板并调用同一保存接口，缺文案时不启动图片下载、不创建成品目录。
- 自动测试：扩展“automatic packaging waits for validated copy text before final image download”；工作台生产记录路径和缺文案状态测试。
- 桌面验收：让图片回复先完成、文案未完成时确认不下载/不打包；文案完成后确认先落盘暂存 TXT，再下载图片，最终 TXT、图片和成品文件夹同时落盘；历史成品缺项时显示“打开成品文件夹（待核对）”。

### UI-ASSISTANT-001｜小猫被 GPT 压住或主题不一致

- 状态：已修复，永久回归。
- 修复标准：小猫使用独立顶层窗口；浅色和深色主题同步；拖动与点击分离；状态气泡不被 GPT 原生视图遮挡。
- 自动测试：全局小猫助手层级和四主题测试。
- 桌面验收：在所有主页面拖动小猫跨过 GPT 区域并切换主题。

### UI-TREE-001｜素材／模板文件夹不能稳定展开、折叠或自动读取

- 状态：已修复，永久回归。
- 修复标准：目录设置后自动扫描；父目录三态选择；多个父目录可同时展开；图片和 TXT 都可预览；大目录按需加载。
- 自动测试：素材扫描、浅层索引、文件树与路径安全测试。

### THEME-001｜深色模式文字、按钮或通知对比度不足

- 状态：已修复，永久回归。
- 修复标准：左侧导航、次级文字、按钮、选中态、小猫气泡和 GPT 背景在四套主题下均可读；按钮子元素继承正确颜色。
- 自动测试：主题结构测试。
- 桌面验收：四套主题逐一检查侧边栏、内容生产、分发、流量转化和设置。

### PROFILE-001｜多浏览器账号隔离与持久恢复

- 状态：已实现基础隔离，持续回归。
- 产品规则：顶部每个“账号窗口 N”对应独立的持久 Chromium 分区 `persist:teambuilding-gpt-production-<id>`，Cookie、登录态、缓存、当前会话、任务阶段和额度组互不串用。
- 修复标准：新增、隐藏、恢复或重命名浏览器不得改变分区 ID；软件升级不得删除分区；同一账号若主动登录到两个浏览器，可选择共用额度组，但浏览器存储仍保持隔离。
- 自动测试：账号窗口档案持久化、多账号任务分配和独立额度测试。
- 桌面验收：账号窗口 1、账号窗口 2 登录不同账号，分别打开不同对话；切换、最小化、重启后检查双方登录和会话均保持原状。

### TEMPLATE-001｜链接模板与文件夹模板双模式

- 状态：已确认产品规则，待当前生产批次结束后实现。
- 链接模式：模板由“名称＋ChatGPT 会话链接＋可选绑定浏览器”构成。开始任务时直接在指定浏览器访问该链接，确认会话可用后仅上传素材；不重复上传母版图片。
- 链接模板真源：用户所选模板库根目录下的 `链接模板.txt`。一行一个模板，默认使用 Tab 分隔：`模板名称<Tab>https://chatgpt.com/c/...<Tab>账号窗口档案ID（可选）`。同时兼容 `模板名称 | 链接` 和首个空白后为链接的简写；空行及 `#` 开头的注释忽略。
- 编辑规则：界面允许手动粘贴名称和链接；新增、修改、删除必须原子写回同一个 TXT。用户在记事本中修改 TXT 后，工作台自动扫描并刷新，不要求点击“读取”。名称或 URL 重复时提示合并／更名，不静默覆盖。
- 文件夹模式：模板由真实本地文件夹构成，包含母版图片及提示词／TXT。开始任务时创建新聊天，先上传模板并初始化母版环境，再逐个上传素材。
- 共同规则：模板区域顶部明确切换两种模式；两种模板可以分别多选；作品数仍按“素材数 × 模板数”计算；同一批任务冻结模板类型和来源；链接模板打不开或账号无权访问时只暂停该模板任务，不串到其他账号或模板。
- 数据安全：只保存模板名称、合法的 `https://chatgpt.com/` 会话 URL、本地路径和账号窗口档案 ID；拒绝非 ChatGPT 域名及非会话链接；不保存 Cookie、Token 或账号密码。写回前先生成同目录临时文件并原子替换，避免断电导致整份列表损坏。
- 自动测试：URL 白名单、模板模式切换、笛卡尔积队列、浏览器绑定、链接失效降级和文件夹初始化测试。
- 桌面验收：1 个链接模板＋1 个文件夹模板 × 2 个复制素材，分别生成 4 个独立任务；确认链接模式没有上传母版、文件夹模式真实上传母版、两个账号不串号。

### WORKFLOW-001｜生产模式必须提供可见、可编辑的阶段流程

- 模式口径：`单账号·手选自动（有提示词）` 保留完整模板/迁移提示词；`单账号·手选自动` 复用当前已沉淀会话，只上传素材、等待计划、发送 `1`、等待图片，再发送可编辑的文案请求词；`单账号单模板·永不停歇` 按同一账号窗口严格一帖一帖循环。

- 状态：已实现可见的完整流程编辑器和依赖校验；可从生产模式选择框右键打开，配置写入模式档案，运行批次冻结当前快照。真实桌面编辑、重启恢复和长时间运行仍待生产账号复验。
- 入口：右键“手动模式／单账号单模板·永不停歇／多账号窗口自动”可打开该模式的流程编辑器；右上角生产设置提供同一入口。两个入口编辑同一份配置。
- 阶段结构：每一步明确显示“阶段名称、触发条件、执行动作、发送文字、等待上限、失败处理、是否启用”。允许新增、复制、停用和在满足依赖时调整顺序。
- 默认单窗口流程：
  1. 任务开始 → 上传当前素材与首轮要求；
  2. 检测到计划稳定完成 → 等待 1–5 秒 → 发送可编辑确认词，默认 `1`；
  3. 检测到本轮图片完成 → 下载本轮图片，同时发送可编辑文案请求词，默认 `给我一份小红书文案`；
  4. 检测到文案完成 → 复制并写入 TXT → 校验图片和文案 → 查重打包；
  5. 打包成功 → 记录成品路径 → 按开关归档素材 → 进入下一套。
- 动作类型：上传素材、等待计划、发送文字、等待图片、下载本轮、请求文案、等待文案、复制 TXT、查重打包、归档、结束／跳过。
- 安全依赖：不得把“发送 1”放到计划完成前，不得在图片和文案校验前打包；界面拖动到无效顺序时直接提示原因，不保存假流程。
- 版本与恢复：流程配置写入工作台 `app-settings.json` 并包含版本号；支持“恢复当前模式默认流程”和导出／导入；不得只保存在浏览器 localStorage。
- 任务冻结：点击开始生产时，把当时的流程保存为本批快照；运行期间修改只作用于下一批，不得改变当前任务。
- 可解释性：生产记录逐阶段写入实际触发条件、发送内容、开始／结束时间和结果，使用户能追溯“这句提示词从哪里来”。
- 自动测试：流程解析、依赖校验、模式隔离、运行快照、默认恢复、设置备份和失败跳过测试。
- 桌面验收：右键三个模式分别编辑；重启后配置仍在；运行中修改不影响当前批；日志能够显示每一步实际发送的内容。

## 新问题登记模板

### TRANSFER-024：已登记但当前离线的设备仍建立传输任务

- 现象：设备曾登记或最近在线，点击文件传送后任务才在底层发现阶段失败。
- 根因：后端对已登记设备直接放行，未在建立任务时复核设备现场记录的 `current` 状态。
- 修复标准：登记设备和临时发现设备都必须命中本轮真实在线记录；最近在线设备只展示且发送按钮禁用。
- 自动测试：`path-security.test.js` 覆盖“已登记但 `current=false` 必须拒绝、当前在线必须放行”。
- 桌面验收：向本轮真实在线设备发送小文件，必须完成 Wi-Fi 上传并收到手机 commit；最近在线设备不得建立任务。
- 数据安全边界：失败准入不创建手机任务、不移动源文件、不修改作品分发账本。
- 状态：已修复永久回归；2026-08-12 向 1 号红米13真机发送 1 文件成功，完整工作台回归 546/546。

### PACKAGE-001：手动打包与自动打包结果不一致

- 现象：自动生产可打包，但用户手动点击“下载并打包”提示“打包程序已结束，但没有找到成品文件夹”。
- 根因：两个入口分别组装请求；手动入口未固定传递当前下载/成品目录，且 PowerShell 中文 stdout 可能被错误解码为另一个合法字符串。
- 修复标准：两种入口共用 `packageDownloadedReply`；服务端按批次号从 UTF-8 `GPT作品记录.json` 恢复权威成品路径；文案或图片不完整时二者都不允许报成功。
- 自动测试：扩展共用函数与禁止 `window.alert`；工作台中文路径恢复、工作包重复与在线目录测试。
- 桌面验收：在同一助手回复先执行“下载本组”，再执行“下载并打包”，确认成品目录存在且不会弹旧版假失败。
- 状态：代码和自动测试已完成，待 0.14.7 开发版桌面复测。

### TEMPLATE-002：在线模板入口缺失或只读分享页无法生产

- 现象：模板区只能选择本地图片文件夹，无法维护名称+会话链接；分享链接打开后没有输入框。
- 修复标准：本地/在线模板一键切换；`链接模板.txt` 可被界面与外部编辑共同读取；只允许 ChatGPT `c/`、`share/` 链接；分享页先点击官方“继续此对话”进入可编辑会话。
- 自动测试：在线模板 URL 白名单、原子写入、增删、前端事件、任务导航和分享页续接检查。
- 状态：代码和自动测试已完成，待真实链接桌面验收。

### BROWSER-002：浏览器标签不能排序/改名，切换干扰生产

- 现象：多个账号标签无法整理；暂停或运行期间切换账号被阻止，或切换后把当前素材注入另一个窗口。
- 修复标准：标签可拖动排序、右键重命名并持久化；切换只改变可视账号，任务始终绑定开始时账号；多账号 worker 一次只处理一个帖子。
- 自动测试：桌面档案排序 IPC、前端拖放/右键入口、任务账号绑定和多账号串行领取。
- 状态：代码和自动测试已完成，待双账号桌面验收。

### FLOW-GPT-004：单窗口继续生产没有重新注入素材

- 现象：单窗口任务在本地额度提醒或桥接前暂停后，点击“继续生成”只进入恢复分支，GPT 页面没有重新出现附件。
- 根因：队列旧任务被统一写入 `retryFromStage`，扩展把它当成已经上传过的网页阶段任务，跳过了文件选择；同时单窗口错误复用了多窗口的本地额度硬闸。
- 修复标准：单窗口本地额度只做提示；桥接前暂停的任务必须从素材树补齐附件并传 `forceUpload=true`；已送达 GPT 的任务才允许按阶段恢复。
- 自动测试：工作台 `160/160`；扩展 `15/15`，包含 `forceUpload` 和重试分支断言。
- 桌面验收：选择一个素材，触发本地额度提醒后暂停，再点击继续；应再次看到真实附件上传，不能只显示“恢复阶段”。
- 数据安全边界：不删除或重建 GPT/Google 登录分区，不清理 Chromium 缓存，不移动素材原文件。
- 状态：已修复，待真实桌面复现确认。

### FLOW-GPT-005：低图数触顶后继续喂素材或只提醒不自动探测

- 现象：本轮只生成 1–3 张后仍继续发送后续素材；或虽停止，但 3 小时后只提醒、不自动用下一条素材试跑。
- 根因：低图数原先只作为任务错误处理；额度提醒计时器没有连接生产队列，且残缺任务仍停留在当前索引。
- 修复标准：1–3 张立即停止；残缺任务失败并跳过；不下载、不文案、不打包、不归档；按第一次进入生图阶段 + 默认 3 小时自动试跑下一条；重启后恢复探测定时器。
- 自动测试：`public/workbench-ui.test.js` 校验低图数识别、索引跳过、自动探测、周期重置和定时器恢复入口。
- 桌面验收：下一次真实额度周期观察一轮 1–3 张触顶；确认小猫提示、队列停止、到点自动试跑下一条，且未清理登录状态。
- 数据安全边界：不删除 Chromium/GPT 分区，不重建账号窗口档案，不移动未成功素材。
- 状态：代码与自动测试已完成；等待下一次真实额度周期桌面验收。

### FLOW-GPT-006：全天自动跨重启与工作时段恢复

- 现象：一批结束后不再选下一批；合盖、断网或重启后即使探测时间已到也不会继续；深夜仍可能启动新作品。
- 修复标准：永不停歇模式持久保存运行意图；启动、联网、唤醒后恢复；默认只在北京时间 07:00 至次日 02:00 启动新作品；休息时段等到下一开始时间；运行中作品不被强杀。
- 自动测试：校验工作时段控件、跨午夜判断、持久运行键、联网/页面恢复监听和启动恢复入口；完整回归 `165/165`。
- 桌面验收：设置一个短跨午夜测试窗口，验证窗口外不发新帖、进入窗口后自动发帖；触顶后重启工作台，验证到点后使用下一条素材。
- 数据安全边界：不得清理 GPT/Google 登录分区、浏览器缓存、素材原目录或成品历史。
- 状态：代码、热更新和自动测试已完成；真实 3 小时跨周期行为需在后续生产中持续观察。

```text
编号：
现象：
首次报告日期：
复现条件：
最早根因：
修复标准：
自动测试：
桌面验收：
数据安全边界：
修复提交：
状态：待复现／修复中／待实测／已修复永久回归
```

### FLOW-GPT-007：多选素材被叠加到同一个 GPT 输入框

- 现象：勾选多个帖子后，自动模式把不同帖子文件夹的图片和 TXT 同时堆进一个 GPT 输入框，而不是一帖完成后再上传下一帖。
- 根因：旧版附件计数扫描整个聊天主区域，把历史回复中的附件误当成当前输入框状态；同时工作台和网页执行器都缺少“一个任务只能包含一个帖子目录文件”的硬边界。网页执行器还曾在自动入口引用作用域外的 `noPromptMode`，导致第一帖上传后异常退出，未发送附件和提示词草稿留在输入框；刷新网页不会清除草稿，后续恢复会再次粘贴同一提示词。
- 修复标准：队列可以保存多个帖子，但单窗口必须严格 `await` 当前作品完整结束；每个素材任务的所有附件必须位于同一个帖子文件夹；当前输入框只要仍有未发送附件或未发送文字，就拒绝注入下一帖；历史聊天附件和已经发送的正文不得参与当前输入框计数。边界冲突必须持久暂停整批并保留当前索引，重启后也不得自动越过冲突帖子。
- 自动化测试：工作台 `public/workbench-ui.test.js` 的单帖子目录边界、草稿/附件边界错误和持久暂停用例；网页执行扩展的 composer 作用域、未发送草稿、残留附件拦截、单目录校验及 `noPromptMode` 作用域用例。
- 桌面验收：2026-08-02 使用两个真实帖子执行单窗口队列。第一帖完整完成 7 图、文案、打包和归档后，第二帖才出现于 GPT；聊天记录中没有提前注入第二帖，也没有重复发送 `1`。第二帖断点恢复后完成 7/7 图片、TXT、打包与归档。
- 数据安全边界：不得清理 GPT/Google 登录分区；拒绝混合上传时不移动素材、不登记使用成功、不伪造完成记录。
- 状态：已修复永久回归；工作台与扩展都保留单帖子边界测试，真实双帖成功闭环已通过。

### FLOW-GPT-008：多图生成被误报为仅 1 张并停止

- 现象：聊天页面最终能看到多张生成图，但自动生产在第一张出现后提示“只生成 1 张”并停止。
- 首次复现：2026-08-01，计划 12 页；旧检测在首图后的短暂停顿约 8 秒时返回，完整回复最终包含 6 个唯一图片文件。
- 根因：旧检测只比较全页图片 URL 增量，并把“页面当前没有停止按钮 + 8 秒无变化”当成整组完成；ChatGPT 多图会逐张到达，图与图之间存在停顿。新版官网还可能把最终套图折叠成回复外层的 `P1…Pn` 沙盒文件按钮，内层正文只能看到少量临时预览图。
- 修复标准：绑定本轮助手回复；优先以原生“复制回复”动作确认整条回复完成；再统计唯一图片地址。沙盒文件按钮必须从该回复的 `conversation-turn` 外层读取和逐个解析，不能全页混入旧回复。没有完成证据且低于安全线时只能暂停当前素材，不能记为额度触顶、跳过素材或发送下一帖。
- 自动测试：扩展覆盖首图停顿、沙盒文件按钮、外层回复容器和预览图/最终文件组去重；工作台断言 `IMAGE_COUNT_UNCERTAIN` 属于保留当前索引的暂停边界。
- 桌面验收：真实第二套计划 7 页，官网最终提供 `P1…P7` 七个文件。旧检测看到 3 张预览图后暂停；热更新从“等待图片”检查点继续，未重新上传、未重复发送 `1`，最终下载 7/7、取得文案、打包并归档成功。
- 数据安全边界：不清缓存、不删除登录分区、不重新上传素材、不重复发送 `1`。
- 状态：已修复永久回归；真实 DOM、断点恢复和完整成品闭环均通过。

### FLOW-GPT-009：10/10 工作包被总计划 12 页误判为不完整

- 现象：GPT 明确把 12 页计划拆成首批 P1–P10，自动流程完成 10 图＋TXT＋作品记录并成功归档；生产历史同步时却再次使用总计划 12 校验，导致已完成作品变成“已暂停／待核对”。
- 根因：工作包已有本批 `expectedImageCount=10` 和 `actualImages=10`，历史接口却只使用检查点的 `plannedImageCount=12`，混淆“整份计划页数”和“本次可发布工作包页数”。
- 修复标准：只有当 `GPT作品记录.json` 状态为 completed、预期数等于实际数、且实际数等于磁盘图片数时，历史校验才采用工作包批次数；任一条件不一致仍按总计划严格判残缺。
- 自动测试：服务器历史接口必须返回本批预期数和“由工作包记录校验”标识；工作台同步后保持 completed，并显示真实 10/10 成品路径。
- 桌面验收：请求 `gpt-1785656798733-crud0t` 同步后仍为 completed，成品目录可打开；删除任一图片或 TXT 后必须恢复为待核对。
- 数据安全边界：不修改现有成品内容、不伪造缺失图片、不改 GPT 计划；只统一完成状态的验证口径。
- 状态：已修复永久回归；热更新后接口返回 `packageValid=true`、`packageExpectedImageCount=10`、`packageValidatedByRecord=true`，工作台同步后状态保持 completed、文案长度 716、成品路径可用。
### BROWSER-003：同一账号窗口的底部地址栏

- 状态：代码和自动测试已完成；开发版页面入口已复核。Electron 原生账号窗口的真实外网跳转、返回和登录态保持需在不运行生产任务时补做桌面验收。

- [x] 右侧 GPT 区底部显示地址输入栏，原生网页不覆盖它。
- [ ] 输入 `https://example.com` 后按回车/点击“访问”，当前账号窗口打开该网页（待 Electron 桌面复验）。
- [x] 输入不带协议的域名会按 https 处理；`localhost`、`127.0.0.1` 和局域网地址按 http 处理。
- [x] `javascript:`, `data:`, `file:`, `devtools:` 及带用户名/密码的 URL 被拒绝。
- [ ] 点击 GPT 首页返回原会话；账号登录状态、GPT 档案和生产队列不被清除（待 Electron 桌面复验）。
- [x] 前进、后退、刷新按钮仍然可用，地址栏在账号切换后显示对应窗口地址（代码已接入，账号切换桌面复验待补）。

## 0.14.23｜浏览器实测验证（2026-08-03）

以下项目通过内置浏览器在 `http://127.0.0.1:4327/#gptProductionTest` 上直接操作验证：

### 模式选择器与提示

- [x] 模式选择器包含 6 个选项：手动模式、自动模式、半自动模式、单账号·永不停歇、多账号轮换·永不停歇、旧版（hidden）。
- [x] 下方动态提示行（`gptModeHint`）实时显示当前模式用途说明，切换模式后文字立即更新。
- [x] `?` 按钮（`gptModeInfoBtn`）点击展开模式详细说明 popover，再次点击收起。
- [x] 5 个模式快速切换标签（手动/自动/半自动/永不停歇/轮换），每个标签显示名称和简短说明。

### 工作流编辑器

- [x] 第一步为固定 init 步骤（"初始化会话"），不可禁用、不可删除、不可拖拽排序。
- [x] 「添加环节」按钮可正常添加新步骤（8→9），新步骤默认类型为 wait-plan。
- [x] 上传步骤（upload-material）包含提示词输入框，placeholder 为"上传提示词（可留空）"，值可为空。
- [x] 等待步骤（wait-plan/wait-images/wait-copy）包含「自动」检测 checkbox 和「启用」checkbox。
- [x] 动作步骤（send-confirm/request-copy）包含可编辑文本输入框，placeholder 为"发送文字内容"。
- [x] 所有步骤均有拖拽手柄（⠿）。
- [x] 每个模式有独立的工作流配置（切换模式后步骤数和内容不同）。
- [x] 母版规则只读文本区域可见（V3.6 迁移器，431 字符）。
- [x] 额外规则 textarea 可编辑，留空时只使用母版规则。

### 状态持久化

- [x] 取消勾选步骤的「启用」后，checkbox 状态持久化到 localStorage（`teambuilding-gpt-mode-profiles-v1`）。
- [x] 工作流步骤文本修改（如"给我一份小红书文案【测试修改】"）在模式切换和面板重开后仍保持。
- [x] 设置存储在 `teambuilding-gpt-auto-settings`，模式档案存储在 `teambuilding-gpt-mode-profiles-v1`。

### CSS 验证

- [x] `.gpt-mode-row select { width: 100% }` CSS 规则正确应用到主模式选择器。
- [x] `.mode-setting-row > label { flex: 1 1 0% }` 使设置面板内选择器自适应宽度。
- [x] 模式选项文字无截断，最长选项"多账号轮换·永不停歇（旧版）"完整显示。

### 仍需 Electron + GPT 环境验证

- [ ] 半自动模式上传素材后，GPT 生成计划时暂停并显示"确认继续出图"按钮。
- [ ] 点击"确认继续出图"后自动完成出图、文案和打包。
- [ ] 等待确认期间发送按钮禁用，暂停/跳过/停止按钮均正确清除 pending。
- [ ] 连续模式状态栏显示"等待额度恢复（约X分钟后探测）"或"工作时段外"。
- [ ] 打开设置面板时 GPT 视图立即隐藏，无残影闪烁（WebContentsView API 仅 Electron 可用）。
- [ ] 取消勾选「启用」后实际生产流程跳过该步骤。
## DISTRIBUTION-AUTO-023｜持续在线设备库存下降后未自动补充（2026-08-10）

- 复现条件：K60 上线时库存不少于阈值，保持在线期间库存下降到 8；设置阈值为 10。
- 根因：旧 `automaticDistributionSessions` 是在线会话级 `Set`，并在实际库存比较前加锁；同一在线会话后续扫描直接跳过。
- 修复标准：库存、阈值、分类、单次发送数或候选可用性变化时重新评估；相同判断状态保持幂等；失败三次后仍等待状态变化或重连。
- 自动测试：库存 15→8、阈值 10→7 的判断指纹必须变化；服务端必须使用 `Map` 保存指纹并写 `evaluated / skipReason` 结构化日志。
- 真实证据：旧版 K60 在重连清锁后以 8<10 触发 `作品集_059[转]`，一次发送成功并回读为 15；0.16.21 需补无需重连的下一次库存变化实测。
- 数据安全边界：不伪造手机库存、不修改已发送账本、不清登录数据；验证只使用真实设备扫描和正常自动发送。

## GPT-ROTATION-SAFETY-022｜账号故障不得烧穿素材队列（2026-08-09）

- [x] 真实复现：账号 1 本地桥接 `Failed to fetch` 后，旧轮换连续把 14 套标记为跳过，真实完成数为 0。
- [x] 账号放行前检查 `productionReady`；未登录、输入框/扩展未就绪时保留当前素材并切换账号。
- [x] `LOCAL_BRIDGE_FETCH_FAILED` 等临时错误使用统一 transient 分类；同任务跨账号保留，全部不可用才暂停。
- [x] 未知故障停在当前素材，不再自动增加队列索引或伪造完成。
- [x] 成功归档素材按稳定 ID 去重，次数目录变化不再导致重复选择。
- [x] 自动测试 364/364。
- [x] 桌面验收：0.15.25 实测账号 1 的忙碌对话不被覆盖，同一素材转交账号 2；13 个附件从原计划恢复，只发送一次 `1`，最终完成 10 PNG + 1 TXT + 1 JSON 并归档。0.15.26 进一步修复“禁止 P11 / 不开第二批”的否定语义误判。

## UI-ASSISTANT-010｜拖动不消失与气泡默认常显（2026-08-09）

- [x] 父子窗口焦点交接增加交互宽限，拖动和透明区域鼠标切换不能立即触发隐藏。
- [x] `bubblePinned` 默认开启；通知轮换结束后保留最后状态，右键菜单和设置中心均可关闭。
- [x] 静音仍立即隐藏气泡，静音结束后按默认设置恢复。
- [x] 自动测试覆盖默认值、界面控件和 Electron 交互保护。
- [ ] 桌面验收：连续左右拖动、跨 GPT 区域拖动、切换工作台页面，确认小猫和气泡无闪烁、无消失。
# 0.16.22 分类库存自动分发

- [x] `autoCategory=conversion` 只读取 `workCounts.conversion`，总数 15、精准 8 时判定 8<10。
- [x] `autoCategory=traffic` 只读取 `workCounts.traffic`；`all` 才读取总数。
- [x] 旧设备仅有 `workCount` 时分类库存为未知，不触发错误补货或错误“库存充足”。
- [x] 机器 JSON 状态保留分类计数，传统 TSV 状态继续兼容。
- [x] 工作台完整自动回归 442/442；新增设备状态测试 2/2；共享传送技能 25/25。
- [ ] K60 确认安装 Android 0.6.17 后，实读精准流量 8 并完成一次真实自动分发。
## GPT-RESTART-SYNC-047：网页完成但重启断点仍显示处理中（2026-08-12）
- [x] 复现：已提交作品在“等待图片”时重启，任务写成 `RESTART_INTERRUPTED`；网页后来完成，旧恢复控制器因错误码不匹配永久跳过。
- [x] 根因：周期恢复只处理 `IMAGE_COUNT_UNCERTAIN`，没有对重启中断的已提交图片检查点做网页完成对账。
- [x] 现场补充：8 图 + 1 TXT 被网页巡检写成 `expectedImageCount=9`，导致 8 张完整图片被判“8/9”；任务真实 `expectedImages=8`。
- [x] 修复：连续模式下双次巡检同一账号、同一任务；只有网页停止生成且图片完成才恢复原检查点。
- [x] 修复：恢复期优先使用任务计划图片数；ChatGPT DOM/输入框可用即清除原生加载状态，不再依赖长期连接触发 `did-finish-load`。
- [x] 自动回归：完成态会恢复，仍在生成态保持暂停，TXT 不计入图片，DOM 可用清除假加载；全量 553/553 通过。
- [ ] 桌面验收：加载 0.18.23 后，让当前重启断点完成一次网页巡检，确认不重传素材并进入文案/下载/归档。
# 全局控件说明计划（2026-08-14）

- [x] 规则：按钮、选择框、输入框、折叠项、拖放区和动态操作控件，鼠标悬停或键盘聚焦超过 3 秒显示说明；移开或失焦立即取消等待。
- [x] 说明统一回答六件事：背景是什么、用途是什么、它是什么、怎么用、如何运行、为什么这样设计。
- [x] P0 覆盖：文件传输、自动检测与分发设置、作品仓库、内容制作生产控制和设置中心高频操作。
- [ ] P1 覆盖：在线分发、流量转化、设备状态与记录筛选。
- [x] 验收：深浅主题可读、不遮挡目标、不改变点击行为、动态列表重新渲染后仍可触发、键盘操作同样可用。

# 文件传输进度入口（2026-08-14）

- [ ] 文件传输页不再显示重复的“进行中任务”统计卡或顶部任务列表。
- [ ] 传输开始后，小猫助手显示任务名称、阶段、百分比和进度条；轮询更新时数值同步变化。
- [ ] 顶部实时状态通知仍出现；完成、失败、取消和历史记录逻辑不变。
- [ ] 无活动任务时助手进度区自动收起，不残留空白容器。

# Moments V1：朋友圈作品库与人工确认发布（2026-08-14）

- [x] 目标好友 10 条/90 张原版 WeFlow 采集逐条落盘；每条有文案、metadata、媒体可见性校验，二次导入不重复。
- [x] WeFlow `.partial` 恢复保留已有 `.enc`；缺少当前动态、解密失败或媒体不可见时停在当前条目并写 traceback，不换下一条。
- [x] 发布器每日一次状态门禁覆盖 `QUEUED`、`PREPARING`、`PREPARED_FOR_HUMAN_CONFIRM`、`CONFIRMED_PUBLISHED`、`FAILED`；UTF-8 BOM 状态文件可读。
- [x] 发布器拒绝坏媒体、文案回读不一致和适配器报告已点击最终发表的结果；V1 代码没有最终发表点击动作。
- [x] `prepare-today --dry-run` 不探测微信；真实 live 在没有 Qt 微信主窗口时于 UI 起步前停止并留存日志/截图。
- [x] 微信主窗口可定位时，未登录状态由 pyweixin 明确返回 `NotLoginError`，流程仍在打开朋友圈前停止，不进入图片选择或发表动作。
- [x] raw/ready 规范化目录已补齐 `asset.json`，保存发布日期、标签和选材开关；`content.txt` 与全部媒体继续成对保留。
- [x] 离线回归覆盖 `current-year`、`anniversary` 和不静默跨策略回退；Moments 定向测试 12/12 通过。
- [x] 历史问题“9 图选择实际只显示 6 图”已通过同盘短路径副本、九宫格数量门禁和稳定等待修复；后续版本升级仍需重复桌面回归。
- [x] 作品确认发布后 `asset.json.usage_count` 递增，重复确认保持幂等；面板和 CLI 对超过 9 张图片的作品提前阻断并返回 `BLOCKED_MEDIA_LIMIT`，不裁剪、不换条。
- [x] 真实微信回归覆盖：登录窗口 → 主窗口类名短暂切换 → Qt 渲染画布；渲染画布通过窗口相对朋友圈入口打开独立编辑窗口，避免固定屏幕坐标。
- [ ] 桌面验收：微信正常显示并登录后，真实打开朋友圈、选择图片、填入文案，确认停在最终“发表”前；人工检查后点击，再执行 `mark-published`。
- [ ] 不配置 Task Scheduler，直到上述人工确认链路在当前微信版本上完成一次。

# 作品仓库阶段与本机配置（2026-08-14）

- [x] 自动回归覆盖仓库 1/2/3 默认标签、`mobile/official/used` 内部阶段、右键重命名、目录选择、目录直接输入和恢复默认入口。
- [x] 深色主题自动回归覆盖作品集前展开按钮的 30×30 圆角方形、hover/focus/active 反馈。
- [x] 已发送阶段继续读取真实 `used` 归档与压缩包入口；本轮未改写分发账本和作品文件。
- [ ] 桌面验收：刷新/重启到 0.18.40 后，分别右键三个仓库标签，确认本机显示名称和绑定地址可保存；确认“仓库 3”卡片可打开已有压缩包。
- [x] 0.18.63 微信前置技能：未运行时按注册路径启动；登录窗口只点击一次“进入微信”并在限定时间等待人工登录；不输入密码、不点击最终发表。
- [x] 0.18.63 登录等待恢复：`WAITING_FOR_HUMAN_LOGIN` 把作品恢复为 `QUEUED`，只能用同一 workId 继续，不自动换下一条。
- [x] 0.18.63 离线回归覆盖 `mmui::LoginWindow`、`mmui::MainWindow`、未知窗口分类和登录等待续跑。
- [x] 2026-08-15 真实微信前置探针：微信进程/窗口可见，但返回 `WECHAT_UIA_OUTER_WINDOW_ONLY`；安全停在 UIA 兼容性边界，没有进入朋友圈内容操作。
- [ ] 0.18.63 桌面回归：关闭微信后点击“发送到微信待发布”，确认微信自动打开；未登录时确认停在登录窗口，人工登录后重新点同一条，继续到朋友圈发表前。
# 0.18.84 素材处理技能中心回归

- [ ] 侧边栏 `data-tab="skills"` 显示“技能中心”，技能页包含 `jianghu-toolbox-material-ingestion` 卡片。
- [ ] `/api/skills` 能返回素材处理状态；缺少 profile、脚本、`关键词作品` 或目标素材库时显示门禁，不执行移动。
- [ ] 预览任务返回 `preview_ready`，结果包含完整帖子、空目录和残留数量，源目录未改动。
- [ ] 确认请求缺少 `confirm` 或 `previewTaskId` 时被拒绝；仅同一条 `preview_ready` 预览允许 commit。
- [ ] 设置中心开关能保存到 `pageSettings.skills.materialIngestionEnabled`，关闭后技能入口不可运行。
- [ ] Node 语法检查、技能路由测试、素材技能测试、工作台/技能 UI 静态测试和全量 `npm test` 通过。
# MOMENTS-093 朋友圈每月一次定时采集

- [x] 技能中心设置能保存月度采集开关、1–28 日和 `HH:MM`。
- [x] `/api/moments/library` 返回 `scheduler.collection`，显示同一 `libraryRoot`、下次运行和最近回执。
- [x] 月度运行键按 `YYYY-MM@HH:MM` 持久化；服务重启或同一分钟轮询不会重复执行。
- [x] 每日自动准备默认使用 `10:20`、每天 1 条，并保留 `PREPARED_FOR_HUMAN_CONFIRM` 人工发表边界。
- [ ] 缺少账号/UID 时记录 `BLOCKED_MISSING_TARGET`，不猜账号、不换目标、不创建第二个素材库。
- [ ] WeFlow 成功采集继续逐条落盘到当前 `D:\朋友圈weflow`，不移动/删除既有作品；真实 WeFlow 月度采集待用户开启后在目标月份验证。
# GPT-SESSION-READY-025｜已有对话开始与手动模板上传（2026-08-18）

- [x] 根因证据：账号 2 页面停在 `https://chatgpt.com/` 加载态；手动模板请求因新请求接管旧请求而连续出现 `gpt-task-aborted`，随后状态/工作流读取超时。
- [x] 自动测试：会话 URL 与页面就绪守卫 `4/4`；工作台静态回归 `302/302`；完整 `npm test --silent` `788/788`；`node --check desktop/main.js`、`node --check public/app.js` 通过。
- [x] 修复契约：保存 `lastConversationUrl`；发送前等待页面就绪；同账号重复点击返回 `GPT_REQUEST_IN_FLIGHT`，不取消首个请求；页面异常保留检查点。
- [x] 桌面基础验收：已在队列停止、无活动 GPT 请求的安全窗口重启桌面；新进程已加载 `0.19.29`，队列/素材/自动设置保留，未重发真实 GPT 任务。
- [ ] 桌面交互验收：在 `2-orlandocardozo706` 上点击一次已有对话“开始生产”和一次手动“上传模板”；确认恢复会话、附件只发送一次并进入扩展工作流，重复点击只显示等待提示。
- 数据安全边界：未改写现有素材/成品，未删除账号登录档案，未在本次回归中再次发送真实 GPT 任务。
# 0.19.180 完整文案检查点精确续接

- [ ] 最近会话日志已截断时，仍能按账号、原 `/c/...` 会话和完整素材路径找回旧检查点。
- [ ] 同一会话里的其他素材、同素材的其他账号均不能被误认。
- [ ] 完整文案续接后直接下载、打包、归档，不再上传素材、补图或修复到新会话。
# 0.19.184 失效对话与账号级故障

- 未发送任务的原 `/c/...` 若加载后明确落在 ChatGPT 首页，可在扩展、输入框、登录态均就绪时创建新对话；新 `/c/...` 必须持久化。
- 已发送任务落到首页时必须阻止重新上传。
- `GPT_PAGE_NOT_READY` 等账号级环境故障耗尽预算后仍保持同一素材和队列游标，不能顺延、隔离或显示“队列已完成”。
# GPT-FORWARD-ONLY-COPY-197｜文案与归档边界强制单向推进（0.19.197）

- [x] 持久化 `copyText/copyTextPath/packagePath` 在通用重启阶段恢复为 `textSubmitted`。
- [x] 页面误显 `plan-ready` 时不清空持久化文案/图片工作流。
- [x] 文案/归档恢复的生成请求上限为 0，旧扩展误报补图也不回退检查点。
- [x] 完整回归 1041 项：1040 通过、1 跳过、0 失败。
- [ ] 08:00 后三条启用流水线出现真实阶段推进与新归档。
# MOMENTS-MONTHLY-WATERMARK-198｜上月首采与账号水位线（0.19.198）

- [x] 首次月度目标按触发日所在月计算为上一个完整月份，跨年月份正确。
- [x] 已有账号水位时从 `collection_cursor_at` 继续，漏跑月份可补齐整个未采集区间。
- [x] WeFlow 时间线按 `createTime` 过滤账号水位到本次截止边界的时间区间，月度任务不再使用手动 10 条上限。
- [x] 账号水位线按 UID 隔离；每条成功处理后持久化运行检查点，整段成功后推进 `collection_cursor_at`，失败可安全重放并去重。
- [x] 已有作品仍通过原指纹去重，失败不消费月度成功运行键。
- [x] 手动采集数量选择保持 10 条/全部历史兼容。
- [x] Node 全量回归通过；月筛选/水位线/导入定向 Python 测试通过。
