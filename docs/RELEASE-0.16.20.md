# 0.16.20 分发可靠性与模板资产登记

## 交付结果

- 分发页顶部控制区吸顶；设备选择使用视口级居中弹窗，支持 Esc、背景滚动锁定和焦点管理。
- 分发历史按“全部 / 手动发送记录 / 自动记录”筛选，统一显示来源、目标、时间和结果，列表采用限定高度内部滚动。
- 自动分发重试次数可在设置中配置，默认 3 次。连续失败后暂停本轮；设备离线再重连后允许重新尝试。
- 修复工作台账号编号与底层真实设备名混用的问题。底层发送现在使用在线扫描返回的设备名，并移除动态作品数后缀。
- 手动分发和自动分发的开始、完成、最终失败接入小猫通知；自动扫描和中间重试不弹通知。
- 新增模板注册表同步器，输出 `templates-registry.json`、`模板记录.md` 和追加式 `模板更新记录.md`。

## 真实分发验收

修复前，自动分发把工作台别名“4号 / 7号”传给底层发送器，连续三次返回“设备匹配数量为 0”，随后按规则暂停本轮。

修复后在真实设备 `vivo` 上连续完成：

- `作品集_056[转]`：任务 `distribution-1786334491870-2krkpl`，进度到 100%，从手机暂存区进入公众号阶段。
- `作品集_057[转]`：任务 `distribution-1786334611749-4ln6vh`，进度到 100%，从手机暂存区进入公众号阶段。

这同时验证了真实设备名传递、自动续补、完成归档和通知事件链。GPT 生产队列全程保持暂停。

## 模板注册表状态

- 本地权威目录：`D:\AICode\项目推进\projects\江湖有旅人\主项目\02-模板库`
- 已登记本地模板：12
- 已映射线上模板：0
- 待上传或绑定线上链接：12

当前没有可用的线上上传接口或有效链接，因此本轮只完成本地登记和差异识别，不把线上状态伪报为完成。

## 回归与安全边界

- `npm test`：439/439 通过。
- `git diff --check`：通过。
- 运行版本：0.16.20；扩展版本：0.2.81。
- 重启前保存生产运行镜像，重启后确认队列仍为暂停且 4 条任务保留。
- 未删除或迁移 Cookie、登录分区、素材、成品、历史记录和现有未跟踪文件。

## 仍待实战的边界

- 0.16.19 成品文案来源旁白质量门仍需在一条新的、安全生产链上实测；现有三个账号对话分别处于待归档或待回复 1 阶段，本轮没有盲目恢复它们。
- 分发页本轮已真实验证自动发送；全新的手动点击发送仍应在下一次用户明确选择目标作品集时补一条实战证据。
