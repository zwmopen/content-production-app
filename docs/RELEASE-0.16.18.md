# 0.16.18 慢速生图恢复与模式证据

## 当前结果

- `IMAGE_COUNT_UNCERTAIN` 不再永久卡住连续生产。后台只读复查原账号的原会话；仍在生成时保持现场，连续两次确认图片完整后恢复同一任务。
- 恢复保持 `_submittedToGpt=true`、`forceUpload=false` 和原 workflow，不重新上传附件、不重复回复 `1`、不提前上传下一套。
- 输入框、附件、计划等其他完整性错误继续硬暂停；应用重载后也不能被普通“页面已就绪”恢复器绕过。
- 服务端生产检查点新增 `productionMode`，后续历史能证明作品由单账号、轮换、定时或巡检模式完成。
- 素材分类主数字显示与本地资源管理器一致的一级文件夹数；展开后明确列出一级与子目录的“可生产、缺 TXT、缺图片、空/不支持、分类容器”，并可点开具体问题目录。
- 素材统计、诊断和自动队列统一排除点号开头的隐藏目录与软链接，不再出现“统计能生产、实际被队列跳过”的口径偏差。
- 素材移动后若后台索引尚未完成，界面会自动复查直至拿到新计数，不再长期停留在旧数字或省略号。

## 真实生产验收

- 请求：`gpt-1786316406983-v08srw`
- 账号：`account-2`
- 会话：`https://chatgpt.com/c/6a72e786-8118-83ee-ae7a-cbdfb37b6b6c`
- 故障现场：等待图片超时，只确认到 1/10，错误为 `IMAGE_COUNT_UNCERTAIN`。
- 恢复结果：网页后来完成 10/10；系统继续请求文案、下载和归档。
- 成品：`20260810_101347_莫干山2天1夜团建🌿漂流+越野+包栋民宿太会玩了`
- 完整性：10 个 PNG、1 个文案 TXT、1 个 `GPT作品记录.json`。
- 素材记账：原素材从 `01-素材库/1` 移动到 `01-素材库/2`，随后队列才前进。

## 回归清单

- [x] 仍在生成时不恢复。
- [x] 完整图片需要两次检查确认。
- [x] 只恢复同一任务和同一账号。
- [x] 队列暂停标志保留到轮换执行器接管，避免重建新队列。
- [x] 非图片完整性错误不自动恢复。
- [x] 服务端保留并返回 `productionMode`。
- [x] `npm test`：429/429。
- [x] `node --check public/app.js`、`public/gpt-runtime-recovery.js`、`server.js`。

## 接手与安全边界

- 当前源码版本：0.16.18。
- 当前真实生产可以继续，不要在单套作品处理中途杀 Electron。
- `server.js` 的 `productionMode` 字段需要在作品安全边界重启本地服务后生效；重启后用下一条完成记录验证 `productionMode=rotate`。
- 本次没有清理 Cookie、登录分区、素材、成品或运行历史。
- 共享第二大脑工作区存在既有未提交改动，本轮不自动覆盖；本文件是本地项目内的当前交付真源。
