# 0.16.24 长迁移计划稳定等待

## 修复内容

- 迁移计划仍在生成时，恢复控制器返回 `wait-current`，不消耗恢复次数。
- 等待期间不发送“继续处理”或重写提示，防止同一附件消息产生重复计划。
- 回复真正稳定结束后，继续沿用既有计划完整性、P1-P10 上限和单次确认检查。
- 图片阶段以真实独立 URL 为准；少于计划数时最多原地补齐两次，仍不足则安全暂停，不进入文案和归档。
- 补图结果跨 GPT 回复累计并持久化：首轮 7 张、补图回复 1 张会按 8/8 完成，重启续接也不会丢掉前序图片。
- 重启恢复按最近一次确认 `1` 划定当前作品边界，只归并本套之后的多条图片回复，不混入历史作品。

## 真实验收

- 账号：account-4。
- 素材：金华周边 9 大漂流，8 张图片＋1 份 TXT。
- 发现：GPT 已输出完整 P1-P10，但网页生成按钮异常残留；旧逻辑重复发送了两次恢复提示。
- 处置：确认正文 7253 字、20 秒不再增长且有完整结束标记后，只结束残留生成状态。
- 结果：系统识别计划并只发送一次 `1`，进入等待图片阶段；未重传素材、未跳过当前帖子。
- 完整闭环：富阳桐洲岛 13 个附件完成 P1-P10 计划，实际检测与下载均为 10/10 张，生成 1212 字文案并完成作品归档、素材计次；该长计划没有触发恢复催促。
- 恢复闭环：杭州萧山小院首轮 7/8、补图回复 1 张，重启后累计恢复 8/8，并完成现成文案复用、8 图下载、作品归档和素材使用层 1→2；队列停在下一套。
- 数据纠正：先前错误归档的金华漂流 5/10 包已保留至 `_待复核/缺图` 并标记 `incomplete`，素材恢复到使用层 1，未删除任何故障证据。
- 自动测试：449/449。
