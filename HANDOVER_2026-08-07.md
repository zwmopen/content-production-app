# 团建工作台工作交接总结

> **历史快照提示**：本文件停留在 0.14.67，仅保留当时的诊断背景。当前唯一权威交接请读取 `docs/HANDOFF.md`，不要依据本文件判断当前版本、进程或下一步。

> **生成时间**：2026-08-07
> **目标**：方便下一个 AI / 工程师快速接手，无需读完整对话历史
> **当前版本**：工作台 0.14.67 / 扩展 0.2.40
> **项目唯一真源**：`D:\AICode\工具开发\projects\teambuilding-workflow-dashboard\`

---

## 1. 项目定位

**江湖有旅人内容生产工作台**——本地化 Electron 应用，给团建业务做：
- 抖音/小红书/公众号内容生产
- 文件分发到手机设备
- 流量转化（公众号草稿、话术库、方案库、用户旅程）
- GPT 单账号/多账号自动生产数字作品（图文 + 小红书文案）

**启动方式**：
```powershell
cd D:\AICode\工具开发\projects\teambuilding-workflow-dashboard\src
npx electron desktop/main.js
```
服务端口：4327

---

## 2. 当前状态快照

- Electron 主进程：**正在运行**（PID 49016，启动于 2026-08-07 08:44:12，窗口标题"团建工作台"）
- 但**仍是旧版本 0.14.66 / 扩展 0.2.39**——0.14.67 / 0.2.40 修复尚未被加载
- 测试结果：**267 pass / 1 fail / 0 skip**
- 工作流模式：用户当前在调试"单账号连续生产"模式

---

## 3. 最近 24 小时的工作进展（按时间倒序）

### ✅ 2026-08-07：v0.14.67 空文件夹只发提示词修复（最新）
- **问题**：重启后某套素材只粘贴了提示词，没上传附件
- **根因**：用户判断正确——本地素材文件夹为空。
  - `resumeExistingWorkflow = !forceUpload && Boolean(planSubmitted || retryFromStage || reconcileAction)` 在 `retryFromStage` 有值时为 true，跳过文件上传检查
  - `handleUploadMaterial` 中 `expectedAttachmentCount === 0` 时直接 `submitComposer()`，只发文本
- **修复**：
  1. `handleUploadMaterial` 第 2579-2589 行：`expectedAttachmentCount === 0` 时抛 `NO_ATTACHMENTS` 错误
  2. `processTask` 第 3451-3456 行：`resumeExistingWorkflow` 改为仅依赖 `planSubmitted`
- **测试**：267 pass / 1 fail（1 个预先存在失败与本次无关）
- **待处理**：用户需**完全重启 Electron**让修复生效

### ✅ 2026-08-07：v0.14.66 ChatGPT 新版统一编辑器发送按钮检测修复
- **问题**：单账号全自动卡在"等待附件就绪 16%"，7 个文件已上传但发送按钮始终未出现
- **根因**：ChatGPT 新版统一编辑器**一个按钮兼做语音和发送**
  - 输入框为空 → 显示语音按钮（aria-label="启动语音功能"，SVG=#voice-regular-24）
  - 有文本 → 显示发送按钮（aria-label="发送提示"，SVG=#send-prompt-style-thin）
  - **仅附件不足以触发发送按钮，必须有文本**
  - React 异步重渲染会清空 `replaceComposerText` 设的文本
- **修复**（`handleUploadMaterial`）：
  1. 新增 `ensureComposerHasPrompt()`：检测到编辑器为空时重新注入提示词；等待循环每 3 秒定期重新注入
  2. `attachmentPreviewCount()` 新增 `[class*="group/file-tile"]` 选择器（ChatGPT 新版无 `data-testid`）
  3. 诊断日志增加 `composerHasText` 字段
- **关键 DOM**：
  - 发送按钮 class：`composer-submit-btn composer-submit-button-color`
  - 语音按钮 class：`composer-submit-button-color text-submit-btn-text`
  - 两者都含 `composer-submit-button`，靠 aria-label 和 SVG 区分

### ✅ 2026-08-06：v0.14.59 诊断 IPC + 自动重试
- `main.js` 新增 `desktop:gpt-diagnostic` IPC handler（`window.tbGptDiagnostic()` 可调）
- `productionReady` 失败时，连续模式下 30 秒自动重试，最多 3 次
- 每 2 分钟检查队列自动恢复

### ✅ 2026-08-06：v0.14.58 扩展唯一真源统一 + server.js 崩溃修复
- 删除外部独立扩展仓库 B（0.2.26，停更）
- `main.js` 的 `resolveGptExtensionPath()` 仅加载 bundled（A，0.2.31+）
- 修复 `conversion.js` 的 `ERR_HTTP_HEADERS_SENT` 崩溃（`headersSent` 防护）

### ✅ 2026-08-06：v0.14.58 全链路对话日志
- 新增 `/api/gpt-production/conversation-log` 接口
- `logConversationEvent()` 在 11 个关键节点记录（upload-sent, plan-received, confirm-sent, images-detected, copy-received 等）

---

## 4. 关键技术概念（接手必读）

### 文件上传三道防线（`sidebar.js` `processTask` 第 3538 行起）
```javascript
const loaded = await Promise.all([loadFiles(paths, task), findFileInput()]);
// 1. DataTransfer on hidden file input (historically reliable)
// 2. Drag-and-drop on composer surface (fallback)
// 3. Paste event (only delivers first file — limitation)
```
**重要**：必须**顺序执行**（不是并行），同时成功会触发"你已上传过此文件"错误。

### 工作流状态机（`sidebar.js` `runAutomaticProduction`）
- `workflow.planSubmitted`：是否已向 GPT 发送计划
- `workflow.planDone`：GPT 是否已完成计划生成
- `workflow.imageSubmitted`：是否已发"1"触发生图
- `workflow.generatedImageUrls`：检测到的图片 URL
- `workflow.textSubmitted`：是否已请求文案
- `workflow.copyText`：GPT 返回的文案全文
- 这些状态由 `state.uploadTasks[i].workflow` 持久化，**重启可续跑**

### 12 步默认工作流（用户可编辑）
1. `upload-material`（上传素材到 GPT）
2. `wait-plan`（等待 GPT 出计划）
3. `send-confirm`（发"1"确认生成）
4. `wait-images`（等待图片生成）
5. `download-images`（下载图片到本地）
6. `request-copy`（请求小红书文案）
7. `wait-copy`（等待文案）
8. `save-text`（保存 TXT）
9. `archive-material`（移动到成品库）
10. `wait-fixed` / `wait-random`（节奏控制）
11. 等等…

### ChatGPT 新版 DOM 选择器速查
```javascript
// 发送按钮（必须文本非空）
'button[aria-label="发送提示"]'
'button[aria-label="发送提示词"]'
'[data-testid="send-button"]'
'[class*="composer-submit-button"]:not([class*="text-submit-btn-text"])'

// 附件 tile（新版用 Tailwind）
'[data-testid*="attachment-tile"]'
'[class*="group/file-tile"]'

// 输入框（ProseMirror）
'[contenteditable="true"][data-prosemirror]'
```

---

## 5. 核心文件路径速查

| 文件 | 作用 |
|---|---|
| `src/integrations/gpt-production-extension/sidebar.js` | **最核心**，扩展主逻辑（2700+ 行） |
| `src/integrations/gpt-production-extension/manifest.json` | 扩展清单，版本号 0.2.40 |
| `src/public/app.js` | 工作台主前端逻辑（~12000 行） |
| `src/public/index.html` | HTML 入口，缓存版本 0.14.67 |
| `src/server.js` | 主服务入口（5114 行，路由已模块化） |
| `src/server/routes/*.js` | 8 个路由模块（juguang/wechat-draft/backup/settings/distribution/production/gpt-extension/conversion） |
| `src/lib/*.js` | 核心数据层（distribution-data, gpt-production-orchestrator 等） |
| `src/desktop/main.js` | Electron 主进程 |
| `src/desktop/preload.js` | Electron 预加载 |
| `VERSION` | 版本号 0.14.67 |
| `gpt-conversation-log.jsonl` | GPT 对话日志（待生成） |

---

## 6. 调试与诊断 API

| API | 方法 | 用途 |
|---|---|---|
| `/api/gpt-production/checkpoint` | GET/POST | 单任务检查点 |
| `/api/gpt-production/conversation-log` | POST/GET | 对话日志 |
| `/api/gpt-production/quota` | GET | 额度状态 |
| `/api/gpt-production/status` | GET | 工作台状态 |
| `/api/gpt-production/dom-snapshot` | POST | DOM 快照 |
| `window.tbGptDiagnostic()` | 浏览器内 | GPT 页面实时状态 |

---

## 7. 当前模式与待办

**用户在跑**：单账号连续生产（连续模式，07:00-02:00 工作时段）

**当前已知问题**：
1. **必须重启 Electron 让 v0.14.67 生效**——这是当前最高优先级
2. 用户素材库中存在"空文件夹"（没附件）——会触发 `NO_ATTACHMENTS` 错误跳过该套
3. 仍有 1 个预先存在的测试失败（`partial GPT attachments are treated as an upload-limit signal`），与本次修改无关

**如果重启后仍有问题，按以下顺序排查**：
1. `window.tbGptDiagnostic()` 确认 GPT 页面是否就绪
2. 检查 `gpt-conversation-log.jsonl`（如果存在）看实际发送内容
3. 检查 Electron 主进程控制台日志（看 `[TB Upload Diag]` 输出）
4. 检查本地素材文件夹是否有实际图片文件

---

## 8. 经验教训（避免重蹈覆辙）

详见 `c:\Users\z\.trae-cn\memory\projects\-d-AICode--p2-fbd73b756049467a2084\project_memory.md`

最关键的几条：
- **`node --test` 必须 `--test-concurrency=1`**——否则 spec reporter 溢出产生假阳性失败
- **测试断言必须随重构同步**——v0.14.50 重构后 3 个断言未更新
- **ChatGPT 文件上传三方法顺序执行**——并行会触发重复上传错误
- **新版 ChatGPT 一个按钮兼做语音/发送**——必须保证文本非空
- **重启后 `planSubmitted` 是判断 resume 的唯一真源**——`retryFromStage` 不可信
- **空附件文件夹必须报错**——不能发送纯文本让 GPT 凭空生成计划

---

## 9. 快速命令清单

```powershell
# 启动
cd D:\AICode\工具开发\projects\teambuilding-workflow-dashboard\src
npx electron desktop/main.js

# 运行测试
cd D:\AICode\工具开发\projects\teambuilding-workflow-dashboard\src
npm test

# 检查端口
Test-NetConnection -ComputerName 127.0.0.1 -Port 4327

# 查 GPT 状态
Invoke-RestMethod -Uri 'http://127.0.0.1:4327/api/gpt-production/quota' -Method Get

# 查 GPT 页面诊断（浏览器 Console）
window.tbGptDiagnostic()
```

---

## 10. 一句话总结

**v0.14.67 已修复"空文件夹只发提示词"的核心 bug，但需要用户完全重启 Electron 才能生效。重启后如果还有问题，先看 `window.tbGptDiagnostic()` 和对话日志。**
